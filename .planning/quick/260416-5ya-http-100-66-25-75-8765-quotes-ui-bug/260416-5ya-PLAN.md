---
phase: quick-260416-5ya-http-100-66-25-75-8765-quotes-ui-bug
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_web/pages.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: false
requirements:
  - GOV-01
  - OPS-01
must_haves:
  truths:
    - "The `/quotes` observation header reads as distinct status, watch, and action bands instead of one crowded block."
    - "The main quote filter and failure-dictionary filter remain usable on desktop, tablet, and mobile without squeezed controls or horizontal overflow."
    - "The profile and inquiry operator sections stay scannable on both desktop and mobile, with forms, notes, and tables visually separated."
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_web/pages.py"
      provides: "Responsive `/quotes` layout and spacing refinements for hero, filters, and operator desks"
      contains: "new wrapper classes and breakpoint rules while preserving existing ids"
    - path: "wxbot/bookkeeping-platform/tests/test_webapp.py"
      provides: "Regression coverage for the new `/quotes` layout hooks"
      contains: "assertions for preserved ids plus new structural class markers"
  key_links:
    - from: "wxbot/bookkeeping-platform/bookkeeping_web/pages.py"
      to: "existing `/quotes` client-side event handlers"
      via: "preserved DOM ids such as `quote-filter-form`, `quote-profile-form`, and `quote-inquiry-form`"
    - from: "wxbot/bookkeeping-platform/tests/test_webapp.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_web/pages.py"
      via: "HTML assertions for the responsive layout wrappers and unchanged interactive anchors"
---

<objective>
Fix the current `/quotes` UI layout bugs without widening scope beyond presentation.

Purpose: make the observation header, filter/forms, and operator desks readable on the real page while keeping quote parsing, validator, publisher, and repair semantics untouched.
Output: one quick execution plan for layout/CSS cleanup plus web regression coverage.
</objective>

<execution_context>
@/Users/newlcc/.codex/get-shit-done/workflows/execute-plan.md
@/Users/newlcc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_web/pages.py
@wxbot/bookkeeping-platform/tests/test_webapp.py

<interfaces>
From `wxbot/bookkeeping-platform/bookkeeping_web/pages.py`:

```python
def render_quotes_page() -> str:
```

Current interactive anchors that must stay stable:

```html
<button type="button" id="quote-refresh-btn">刷新</button>
<div id="quote-watchlist-grid" class="quote-watchlist-grid">
<section class="quote-promotion-panel" id="quote-promotion-gate">
<form id="quote-filter-form" class="quote-filter-grid">
<section class="panel stack panel-config" id="quote-profile-panel">
<form id="quote-profile-form" class="quote-desk-form">
<section class="panel stack panel-assist" id="quote-inquiry-panel">
<form id="quote-inquiry-form" class="quote-desk-form">
<form id="quote-failure-dictionary-form" class="quote-filter-grid">
```

Executor should preserve these ids and existing form field names so the embedded JS keeps working.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reflow the `/quotes` observation header and filter bands</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/pages.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <behavior>
    - Test 1: `/quotes` renders dedicated structural wrappers for the observation header so the experimental banner, watchlist/promotion context, and command/filter area are not one continuous dense block.
    - Test 2: The main quote filter and failure-dictionary filter expose layout hooks that support wrap/reflow at desktop, tablet, and mobile breakpoints.
    - Test 3: Existing ids used by client-side JS remain present after the reflow.
  </behavior>
  <action>In `render_quotes_page()` and the shared CSS block, introduce explicit layout wrappers around the top observation area and filter area so the header reads in tiers instead of one crowded stack. Rebalance spacing, column widths, and breakpoint behavior for `.quote-hero`, `.quote-hero-grid`, `.quote-ops-metrics`, `.quote-watchlist`, `.quote-promotion-panel`, `.quote-filter-grid`, and the failure-dictionary search form. Preserve all existing interactive ids, anchors, and wording semantics tied to experimental-wall governance per `GOV-01`; this is a layout pass, not a feature or copy rewrite.</action>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp.WebAppTests.test_quotes_page_renders_board_and_exception_sections -v</automated>
  </verify>
  <done>The top of `/quotes` is visually chunked into readable bands, filters stop squeezing into unreadable columns, and no existing JS hook is renamed or removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reduce operator-desk density for profile and inquiry sections</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/pages.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <behavior>
    - Test 1: The profile and inquiry panels render distinct intro/form/table subareas rather than one uninterrupted dense column.
    - Test 2: Desktop keeps a balanced two-panel operator layout, while tablet/mobile collapse the desk grids cleanly without cramped four-column fields.
    - Test 3: Existing form ids, button ids, and table ids for profile/inquiry workflows remain intact.
  </behavior>
  <action>Refine `.ops-grid`, `.toolbar`, `.quote-desk-form`, `.quote-desk-block`, `.quote-desk-grid`, `.quote-desk-grid-3`, `.quote-desk-grid-4`, `.quote-desk-actions`, and related table-head spacing so the operator sections scan cleanly on both desktop and mobile. Add only the minimum structural wrappers/classes needed to separate notes, form blocks, and current-state tables. Do not change parser-template fields, save/delete flows, inquiry semantics, or any backend/API behavior; keep the current forms and ids stable per `OPS-01` and the repo's live-bug boundary.</action>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp.WebAppTests.test_quotes_page_renders_board_and_exception_sections -v</automated>
  </verify>
  <done>The profile and inquiry desks no longer feel like a compressed wall of controls, and the same workflows remain reachable with unchanged ids and endpoints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the live `/quotes` page layout against the known pain points</name>
  <what-built>Responsive `/quotes` layout refinements for the crowded observation header, filter bands, and profile/inquiry operator sections.</what-built>
  <how-to-verify>
    1. Open `http://100.66.25.75:8765/quotes`.
    2. Check the top of the page on desktop width: the experimental status, watch/promotion context, and filter area should read as separate blocks rather than one crowded mass.
    3. Narrow the viewport to tablet/mobile widths and confirm the main filter, failure-dictionary filter, profile form, and inquiry form stack without squeezed inputs or overlapping buttons.
    4. Confirm the profile and inquiry sections still expose the same actions and tables, with no missing controls.
  </how-to-verify>
  <resume-signal>Type `approved` if the layout is clean, or list the remaining UI defects by section.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered HTML -> existing `/quotes` client JS | Layout changes must not break ids/selectors that embedded JS depends on |
| responsive CSS -> operator workflow usability | Poor breakpoint choices can make real operational controls unusable even if APIs still work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-01 | T | `/quotes` DOM structure | mitigate | Preserve current form/button/table ids and only add wrapper classes around them |
| T-quick-02 | D | responsive filter/forms | mitigate | Add breakpoint-specific grid rules so controls reflow instead of overflowing or collapsing unreadably |
| T-quick-03 | I | experimental-wall operator messaging | mitigate | Keep existing governance wording factual and avoid implying new authority or changed publish semantics |
</threat_model>

<verification>
Automated proof: `tests.test_webapp.WebAppTests.test_quotes_page_renders_board_and_exception_sections` passes after the new wrappers/classes are asserted.

Human proof: the live `/quotes` page no longer exhibits the three reported layout issues at desktop and mobile widths.
</verification>

<success_criteria>
The quick fix is complete when `/quotes` is easier to scan at the top, both filter areas remain usable across breakpoints, the operator desks are visually separated instead of dense, and no quote-wall semantics or JS-driven workflows change.
</success_criteria>

<output>
After completion, create `.planning/quick/260416-5ya-http-100-66-25-75-8765-quotes-ui-bug/260416-5ya-SUMMARY.md`
</output>
