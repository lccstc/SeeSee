---
phase: 05-exception-repair-state-machine
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/database.py
  - wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - EXCP-01
  - EXCP-02
  - EXCP-03
must_haves:
  truths:
    - "Repair work accumulates attempt history and failure logs instead of overwriting earlier failures."
    - "Repair-case states are explicit, machine-readable, and escalation-ready after repeated failure."
    - "Existing exception surfaces can expose linked repair-case summaries without adding remediation execution or publish controls."
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py"
      provides: "Explicit repair-case transition helpers and append-only attempt recording."
      contains: "def record_quote_repair_attempt("
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/database.py"
      provides: "Repair-case summary queries and state/update helpers for exception surfaces."
      contains: "def list_quote_exceptions("
    - path: "wxbot/bookkeeping-platform/tests/test_webapp.py"
      provides: "Exception API regressions proving repair summaries are visible without new bypass controls."
      contains: "repair_case"
  key_links:
    - from: "quote_repair_case_attempts"
      to: "quote_repair_cases"
      via: "append-only attempt history and state rollups"
      pattern: "attempt_number|outcome_state|failure_note"
    - from: "wxbot/bookkeeping-platform/bookkeeping_core/database.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      via: "existing `/api/quotes/exceptions` payload"
      pattern: "repair_case"
---

<objective>
Finish the Phase 05 substrate by making repair attempts cumulative, stateful, and escalation-ready without implementing the Phase 06 remediation loop.

Purpose: satisfy D-13, D-14, and D-15 by adding append-only attempt history, explicit state transitions, and linked repair summaries on existing exception surfaces while keeping publisher and snapshot semantics deferred.
Output: cumulative attempt recorder, machine-readable transition helpers, failure-log stacking, escalation-ready rollups, and exception payload summaries for future operator/remediation phases.
</objective>

<execution_context>
@/Users/newlcc/.codex/get-shit-done/workflows/execute-plan.md
@/Users/newlcc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-exception-repair-state-machine/05-CONTEXT.md
@.planning/phases/05-exception-repair-state-machine/05-RESEARCH.md
@.planning/phases/05-exception-repair-state-machine/05-VALIDATION.md
@wxbot/bookkeeping-platform/bookkeeping_core/database.py
@wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
@wxbot/bookkeeping-platform/tests/test_postgres_backend.py
@wxbot/bookkeeping-platform/tests/test_webapp.py

<interfaces>
Required Phase 05 states:

```text
detected
packaged
baseline_ready
ready_for_attempt
attempt_failed
attempt_succeeded
escalated
closed_resolved
closed_ignored
```

Baseline attempt contract from 05-02:

```text
attempt_kind='baseline'
attempt_number=0
baseline_attempt_id -> quote_repair_case_attempts.id
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add append-only attempt recording and escalation-ready state transitions</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <behavior>
    - Test 1: later attempts increment `attempt_number` without modifying baseline attempt `0`.
    - Test 2: failure logs stack cumulatively, and repeated failed attempts move the case through `ready_for_attempt -> attempt_failed -> escalated` without overwriting earlier notes.
    - Test 3: a successful attempt can transition to `attempt_succeeded` while still deferring any actual profile absorption or publisher mutation.
  </behavior>
  <action>Implement explicit state and attempt recording per D-13, D-14, D-15, D-16, and D-17. Add append-only helpers such as `record_quote_repair_attempt(...)`, `advance_quote_repair_case_state(...)`, and summary-rollup helpers that compute `attempt_count`, `last_attempt_outcome`, `failure_log_json`, `escalation_state`, and `closed_at` from attempt history. Enforce legal transitions between `packaged`, `baseline_ready`, `ready_for_attempt`, `attempt_failed`, `attempt_succeeded`, `escalated`, `closed_resolved`, and `closed_ignored`. Keep baseline attempt immutable. Do not schedule retries, do not cap retries automatically, and do not apply fixes to `quote_group_profiles` or shared parsers; that remains Phase 06.</action>
  <acceptance_criteria>
Attempt rows are append-only and baseline attempt `0` is immutable.
Repair-case state transitions are machine-readable and validated in code.
Failure logs accumulate rather than being overwritten on the case row.
Escalation readiness is visible in case state without executing any remediation loop.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend -v</automated>
  </verify>
  <done>The repair substrate now preserves cumulative history and explicit transitions instead of collapsing repeated failures into mutable note text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface linked repair-case summaries on existing exception APIs only</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <behavior>
    - Test 1: `/api/quotes/exceptions` includes linked repair summary fields such as `repair_case_id`, `lifecycle_state`, `attempt_count`, `baseline_attempt_id`, and `escalation_state`.
    - Test 2: existing exception-save and resolution endpoints (`harvest-save`, `result-save`, resolve, reopen, ignore) keep their current business behavior but now also synchronize linked repair-case state and append attempt history where replay occurs.
    - Test 3: the exception list can show repeated failure state without exposing a bypass around validator, publisher, or snapshot confirmation logic.
  </behavior>
  <action>Use existing web/db surfaces per D-05, D-06, D-18, D-19, D-20, and D-21. Extend `list_quote_exceptions(...)` and `/api/quotes/exceptions` so each exception row can carry a compact linked repair summary sourced from `quote_repair_cases` and `quote_repair_case_attempts`: case ID, lifecycle state, attempt count, last attempt outcome, escalation state, and baseline attempt ID. In the same plan, explicitly wire the current exception-save and exception-resolution handlers into repair-case state sync: when `harvest-save`, `result-save`, replay reuse, resolve, reopen, or ignore changes an exception lifecycle, the linked repair case must also transition or append the corresponding attempt/failure-log entry instead of leaving repair history stale. Keep this state-sync only. Do not add new POST routes, remediation triggers, publish actions, or snapshot-confirmation UI. This plan stops at read-only substrate exposure plus brownfield state consistency so Phase 07 can build the operator workbench on top of it later.</action>
  <acceptance_criteria>
Existing exception list responses include linked repair summary fields.
No new web control can trigger remediation, publish, or snapshot execution.
Web regressions prove the summary is visible and existing exception workflows keep repair-case state/attempt history synchronized.
The read surface is enough for later operator/remediation phases without creating a bypass today.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp -v</automated>
  </verify>
  <done>Repair state is now visible on existing exception surfaces while remaining a read-only substrate, not an execution path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| later remediation attempts -> repair history | future remediation writes must not rewrite prior attempt evidence |
| exception API -> operator surface | internal repair state becomes visible on existing web payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | T | attempt history | mitigate | Make attempts append-only, preserve immutable baseline attempt `0`, and store cumulative failure logs separately from current state rollups. |
| T-05-03-02 | R | state transitions | mitigate | Encode allowed lifecycle transitions in code and persist transition timestamps/outcomes so escalation decisions are auditable. |
| T-05-03-03 | I | exception API repair summary | mitigate | Expose compact repair summary fields only; keep raw message snapshots and detailed attempt payloads out of the list endpoint until later operator views need them. |
| T-05-03-04 | E | Phase 05 surface creep | mitigate | Add read-only summary exposure only; do not add publish/remediation/snapshot controls, leaving execution authority to Phase 06, Phase 03, and Phase 04. |
</threat_model>

<verification>
Wave 3 is complete only when append-only attempt history, legal state transitions, and read-only exception summaries are all covered by PostgreSQL-backed and web regressions.
</verification>

<success_criteria>
Phase 05 ends with a complete repair substrate: durable cases, immutable baseline replay, cumulative attempts, explicit escalation-ready states, and read-only summaries on existing exception surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/05-exception-repair-state-machine/05-03-SUMMARY.md`
</output>
