---
phase: 05-exception-repair-state-machine
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/database.py
  - wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/sql/postgres_schema.sql
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - EXCP-02
  - EXCP-03
must_haves:
  truths:
    - "Each packaged repair case can produce one immutable baseline replay tied to the owning group's current grammar state."
    - "Before/after replay comparison is durable, candidate-only, and tells the operator whether the grammar is better, same, worse, or blocked."
    - "Baseline replay never implies publish authority and never mutates active quote facts."
  artifacts:
    - path: "wxbot/bookkeeping-platform/sql/postgres_schema.sql"
      provides: "Additive `quote_repair_case_attempts` persistence schema."
      contains: "CREATE TABLE IF NOT EXISTS quote_repair_case_attempts"
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py"
      provides: "Baseline replay orchestration and before/after comparison helpers."
      contains: "def create_baseline_repair_attempt("
    - path: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      provides: "Shared candidate-only replay surface reused by repair-case baseline creation."
      contains: "_replay_latest_quote_document_with_current_template"
    - path: "wxbot/bookkeeping-platform/tests/test_webapp.py"
      provides: "Replay-baseline regressions proving candidate-only behavior and durable comparison output."
      contains: "baseline"
  key_links:
    - from: "wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      via: "shared replay helper or extracted replay wrapper"
      pattern: "replay_latest_quote_document"
    - from: "quote_repair_case_attempts"
      to: "quote_documents / quote_validation_runs"
      via: "baseline attempt lineage"
      pattern: "quote_document_id|validation_run_id|replayed_from_quote_document_id"
---

<objective>
Add the deterministic baseline replay and comparison substrate that proves whether a repair case's owning group grammar is better, same, worse, or blocked under the current profile.

Purpose: satisfy D-10, D-11, and D-12 by persisting one immutable baseline attempt per case and comparing replay output without implying any active-fact mutation.
Output: additive `quote_repair_case_attempts` schema, baseline-attempt helpers, and replay comparison data persisted through existing candidate/validator surfaces.
</objective>

<execution_context>
@/Users/newlcc/.codex/get-shit-done/workflows/execute-plan.md
@/Users/newlcc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-exception-repair-state-machine/05-CONTEXT.md
@.planning/phases/05-exception-repair-state-machine/05-RESEARCH.md
@.planning/phases/05-exception-repair-state-machine/05-VALIDATION.md
@.planning/phases/01-candidate-contract-foundation/01-03-SUMMARY.md
@.planning/phases/02-validation-engine/02-03-SUMMARY.md
@wxbot/bookkeeping-platform/bookkeeping_core/database.py
@wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
@wxbot/bookkeeping-platform/tests/test_webapp.py

<interfaces>
From `bookkeeping_web/app.py`:

```python
def _replay_latest_quote_document_with_current_template(
    db: BookkeepingDB,
    *,
    exc_row: dict,
    record_exceptions: bool = True,
) -> dict
```

Returned payload today includes:

```python
{
    "replayed": True | False,
    "rows": int,
    "exceptions": int,
    "quote_document_id": int,
    "validation_run_id": int,
    "remaining_lines": list[str],
    "mutated_active_facts": False,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add immutable baseline-attempt persistence and lineage</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py, wxbot/bookkeeping-platform/sql/postgres_schema.sql, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <behavior>
    - Test 1: each repair case can create attempt `0` exactly once, and the case stores `baseline_attempt_id` after that baseline exists.
    - Test 2: baseline attempts persist replay document ID, validation run ID, group-profile snapshot JSON, remaining-line evidence, and summary JSON without overwriting origin evidence.
    - Test 3: cases blocked by missing group profile or missing template config still get deterministic baseline status recorded as machine-readable attempt outcome rather than freeform notes.
  </behavior>
  <action>Create additive attempt history storage per D-07, D-08, D-10, and D-11. Add `quote_repair_case_attempts` with immutable baseline attempt support: `repair_case_id`, `attempt_kind`, `attempt_number`, `trigger`, replay `quote_document_id`, `validation_run_id`, `replayed_from_quote_document_id`, `group_profile_id`, frozen `profile_snapshot_json`, `remaining_lines_json`, `attempt_summary_json`, `outcome_state`, and `failure_note`. Add DB/service helpers to create a baseline attempt once, link it back through `quote_repair_cases.baseline_attempt_id`, and refuse duplicate baseline creation for the same case. Persist only IDs plus frozen mutable snapshots; do not duplicate `quote_validation_row_results` or candidate rows into another mutable copy.</action>
  <acceptance_criteria>
`quote_repair_case_attempts` exists and supports one immutable baseline attempt per case.
Baseline attempts reference replay `quote_documents` and `quote_validation_runs` rows by foreign key.
Cases store `baseline_attempt_id` only after a baseline attempt is persisted.
Blocked baseline prerequisites are represented as structured attempt outcomes, not ad hoc strings on the case row.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend -v</automated>
  </verify>
  <done>The repo has durable baseline-attempt storage that can prove exactly what replay was used as the repair case's immutable starting point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist candidate-only baseline replay and before/after comparison output</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py, wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <behavior>
    - Test 1: baseline replay reuses the existing candidate-only replay surface and persists a replay document plus validation run with `mutated_active_facts = False`.
    - Test 2: the stored attempt summary records origin-vs-baseline row counts, message decision, exception count, remaining lines, and a comparison classification such as `better`, `same`, `worse`, or `blocked`.
    - Test 3: baseline or later repair-attempt replay never opens a second canonical repair case for the same origin exception; replay-side exceptions are either suppressed from case creation or linked as child evidence to the same `repair_case_id`.
    - Test 4: existing harvest/replay flows continue to stay candidate-only while becoming reusable proof surfaces for repair-case baselines.
  </behavior>
  <action>Per D-10, D-11, D-12, and D-20, reuse `_replay_latest_quote_document_with_current_template(...)` as the proof engine for baseline creation. Extract or wrap shared replay logic as needed so repair-case baseline creation can persist: replay `quote_document_id`, replay `validation_run_id`, current `remaining_lines`, validator/message summary, and a comparison block against the origin document/run. Add an explicit replay-to-case contract: baseline and later repair-attempt replays must not open new canonical repair cases. They must either execute with repair-case creation suppressed for replay-side exceptions or persist replay-side exceptions as child evidence linked back to the same `repair_case_id`. The comparison must stay candidate/validator oriented in this phase; it may classify outcomes but must not close cases automatically, update group profiles, write `quote_price_rows`, or alter snapshot execution semantics. Keep any wording and payloads explicit that replay is proof-only and fact-neutral.</action>
  <acceptance_criteria>
Baseline replay uses the current bound group profile and persists its replay document and validation run.
Attempt summaries expose durable before/after comparison data without inferring publish success.
Replay remains candidate-only with `mutated_active_facts = False`.
Replay baselines and later attempt replays do not fork duplicate canonical repair cases from replay-generated exceptions.
Web regressions prove baseline persistence, replay-to-case linking, and comparison output on top of existing replay behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp -v</automated>
  </verify>
  <done>Repair cases now have a durable baseline replay and before/after comparison substrate that later remediation can consume safely.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repair-case baseline service -> replay helper | durable repair-case state calls a replay path that must stay candidate-only |
| baseline attempts -> PostgreSQL lineage tables | mutable group grammar is frozen and linked to replay/validation artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | T | baseline replay lineage | mitigate | Persist immutable attempt rows with replay document/run IDs and frozen profile snapshots so later comparisons cannot be silently reinterpreted from live state. |
| T-05-02-02 | R | before/after comparison | mitigate | Store comparison summaries on attempt rows rather than leaving them in transient API payloads, preserving auditability for future remediation. |
| T-05-02-03 | E | replay helper | mitigate | Reuse the candidate-only replay path and assert `mutated_active_facts = False`; baseline creation must not call publisher or write `quote_price_rows`. |
| T-05-02-04 | D | baseline generation | mitigate | Enforce one baseline attempt per case and represent blocked prerequisites as structured attempt outcomes, preventing repeated baseline churn and hidden failure states. |
</threat_model>

<verification>
Wave 2 is complete only when baseline attempts persist durable replay lineage and the web replay suite proves the comparison substrate stays fact-neutral.
</verification>

<success_criteria>
Every packaged repair case can be judged against one immutable baseline replay without any ambiguity about evidence lineage or accidental publish authority.
</success_criteria>

<output>
After completion, create `.planning/phases/05-exception-repair-state-machine/05-02-SUMMARY.md`
</output>
