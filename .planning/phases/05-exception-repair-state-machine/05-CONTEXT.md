# Phase 05: Exception Repair State Machine - Context

**Gathered:** 2026-04-14
**Status:** Ready for planning

<domain>
## Phase Boundary

Phase 05 exists to turn the current exception pool from a passive failure list into a durable repair-case system. The system already captures raw message evidence, candidate bundles, validator runs, replay attempts, and group profiles. This phase must connect those pieces into one repair object with explicit lifecycle state, replay baseline, and cumulative failure history so later auto-remediation has a safe substrate to work on. It does not grant publish authority, does not change `full_snapshot` / `delta_update` execution semantics, and does not allow agents or scripts to mutate active quote facts.

</domain>

<decisions>
## Implementation Decisions

### Repair case is the new unit of exception handling
- **D-01:** Every failed, partial, rejected, or unclassifiable quote parse must become a durable repair case, not just a disposable exception row.
- **D-02:** The repair case is the canonical object for later subagent remediation, replay comparison, and escalation. Later phases should not need to reconstruct context ad hoc from scattered tables.
- **D-03:** Phase 05 should build on the existing `quote_parse_exceptions` evidence instead of inventing a parallel exception-capture pipeline disconnected from runtime and replay.

### System-led failure packaging
- **D-04:** Repair cases must be generated by system-detected failures such as `missing_group_template`, `strict_match_failed`, validator `held`/`rejected`, zero-publish candidate runs, or unresolved message outcomes.
- **D-05:** The system, not the model, decides when a repair case is opened, updated, retried, escalated, or closed.
- **D-06:** Models and subagents may later inspect repair cases, but they must consume a standard package assembled by the system rather than freeform logs.

### Replay-critical evidence must travel with the repair case
- **D-07:** A repair case must carry enough context to rerun and compare a message without manually rediscovering inputs.
- **D-08:** Minimum repair evidence includes: raw message, source line/unmatched evidence, current group profile/template version, candidate bundle reference, validator run reference, replay baseline, failure reason(s), and prior attempt history.
- **D-09:** The repair package must preserve owning group identity and group-profile versioning because this project evolves one group grammar at a time rather than chasing a global parser.

### Before/after replay is the proof surface
- **D-10:** Phase 05 must define a deterministic baseline replay for the owning group profile so a later proposed fix can be judged as “better”, “same”, or “worse”.
- **D-11:** Before/after replay comparison is required because fixing one exception is not enough; the system must know whether the group grammar improved safely.
- **D-12:** The comparison surface should stay candidate/validator oriented in this phase. It must not imply that a better replay automatically updates active facts.

### Cumulative history and bounded escalation
- **D-13:** Repair work is iterative. The system must retain cumulative attempt history and failure-log stacking instead of overwriting earlier failures.
- **D-14:** State should be explicit and machine-readable, supporting at minimum: newly detected, packaged, ready for attempt, attempt failed, attempt succeeded, escalated, and closed/resolved.
- **D-15:** Phase 05 should make repeated failure visible and escalation-ready, but the actual bounded subagent loop belongs to Phase 06.

### Group-first remediation philosophy
- **D-16:** The system should preserve owning-group context so later remediation can prefer group profile / group section / bootstrap fixes before shared parser changes.
- **D-17:** A repair case is not just “an error”. It is evidence that a specific group's grammar is incomplete or a shared deterministic rule is insufficient.
- **D-18:** Nothing in this phase should push the architecture back toward a global万能解析器 or prompt-defined rules.

### Explicit non-goals
- **D-19:** Phase 05 does not create the auto-remediation loop itself; it prepares the state machine substrate for Phase 06.
- **D-20:** Phase 05 does not change guarded publisher ownership; active quote mutation remains out of scope until Phase 03.
- **D-21:** Phase 05 does not redefine snapshot semantics or operator confirmation flows; those remain later-phase concerns.

### the agent's Discretion
- Whether repair-case state is implemented as additive columns on `quote_parse_exceptions`, a dedicated `quote_repair_cases` table, or a hybrid with linked attempt tables
- Exact replay-baseline representation as long as it is durable, deterministic, and tied to owning group profile/template lineage
- Exact API/test surface for before/after comparison, provided it is consumable by later remediation and operator flows

</decisions>

<specifics>
## Specific Ideas

- The current exception pool already holds real customer failures and is the natural seed for repair cases.
- Runtime and replay already persist candidate bundles and validator runs, so Phase 05 should reference those IDs instead of duplicating payloads blindly.
- The web layer already has replay and harvest-save paths; they should eventually read/write repair-case state rather than treating exceptions as one-off rows.
- Later subagents should receive a standard repair package that includes current group profile/template data, current replay baseline, and stacked prior failure notes.

</specifics>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Product and governance
- `.planning/PROJECT.md` — Hard rules for candidate-only parsing, group-profile evolution, and system-led repair workflow
- `.planning/REQUIREMENTS.md` — `EXCP-*` requirements and the non-goal of a global万能解析器
- `.planning/ROADMAP.md` — Phase 05 scope, success criteria, and dependency ordering before publisher work
- `.planning/STATE.md` — Current project focus after finishing Phase 02.1
- `AGENTS.md` — Repo-level quote-wall risk posture and one-group-one-template operating rules

### Prior phase outputs
- `.planning/research/REAL-EXCEPTION-CORPUS.md` — Live exception evidence and dominant failure buckets that feed repair-case inputs
- `.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-VERIFICATION.md` — Proven corpus and candidate coverage baseline that Phase 05 must preserve
- `.planning/phases/02-validation-engine/02-VERIFICATION.md` — Validator-owned `publishable_rows` and durable validation-run behavior that repair cases should reference

### Existing quote-wall implementation
- `wxbot/bookkeeping-platform/bookkeeping_core/database.py` — Current `quote_parse_exceptions`, `quote_validation_runs`, `quote_validation_row_results`, and `quote_group_profiles` persistence
- `wxbot/bookkeeping-platform/bookkeeping_core/quotes.py` — Runtime candidate generation and validation wiring that produces exception and candidate evidence
- `wxbot/bookkeeping-platform/bookkeeping_core/quote_validation.py` — Durable row/message decisions that should become part of repair packages
- `wxbot/bookkeeping-platform/bookkeeping_web/app.py` — Replay, exception harvest-save, and candidate-only replay behaviors that Phase 05 should reuse rather than bypass
- `wxbot/bookkeeping-platform/tests/test_quote_exception_corpus.py` — Existing real-sample corpus surface that can seed repair-case tests

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `quote_parse_exceptions` already stores raw text, source line, reason, parser lineage, message time, and resolution fields
- `quote_validation_runs` and `quote_validation_row_results` already provide durable validator-owned decision surfaces
- `quote_group_profiles` already holds the owning group grammar context needed for group-first remediation
- Replay already creates candidate bundles plus validation runs without mutating active facts

### Established Patterns
- Runtime and replay both persist candidate bundles first, then validation runs, then exception evidence. Phase 05 should attach repair-case state to this sequence rather than inventing a separate ingestion path
- Quote-wall work increasingly uses additive schema and durable audit surfaces, not opaque in-memory state

### Integration Points
- Runtime exception recording in `bookkeeping_core/quotes.py`
- Replay exception recording and harvest-save flows in `bookkeeping_web/app.py`
- Candidate/validation persistence in `bookkeeping_core/database.py`
- Real-sample corpus fixtures and regression suites under `wxbot/bookkeeping-platform/tests/`

### Risk Hotspots
- Existing `quote_parse_exceptions` rows currently mix open/resolved/ignored semantics but do not yet express attempt state, replay baseline lineage, or escalation readiness
- Replay exists, but there is not yet a durable before/after comparison object that later remediation can trust
- If Phase 05 duplicates too much payload instead of linking durable IDs, repair cases will drift from the current candidate/validator truth
- If Phase 05 tries to include publish behavior, it will overlap Phase 03 and blur the system boundary

</code_context>

<deferred>
## Deferred Ideas

- Bounded subagent remediation attempts, retry cap, and failure-log-driven repair proposals remain Phase 06
- Guarded publisher and active-fact protection remain Phase 03
- Snapshot/delta execution semantics remain Phase 04
- Operator-facing repair workbench remains later UI/operator phases, though Phase 05 should create the data model they will consume

</deferred>

---

*Phase: 05-exception-repair-state-machine*
*Context gathered: 2026-04-14*
