---
phase: 07-operator-verification-failure-dictionary
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/database.py
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/bookkeeping_web/pages.py
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
  - .planning/research/FAILURE-DICTIONARY.md
autonomous: true
requirements:
  - INDU-03
  - OPS-01
must_haves:
  truths:
    - "Repeated repair failures are indexed into a searchable failure dictionary / repair lexicon instead of remaining a blob of attempt logs."
    - "Fresh agents can retrieve symptoms, preferred scopes, forbidden fixes, and known-good repairs without prior session context."
    - "Dictionary knowledge remains linked to real repair cases, fixtures, and tests."
  key_links:
    - from: "quote_repair_cases / quote_repair_case_attempts"
      to: "failure dictionary entries"
      via: "case files aggregate into handbook entries"
    - from: "operator workbench"
      to: "failure dictionary"
      via: "operators can inspect a case and then jump to the relevant lexicon entry"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/database.py"
      provides: "Searchable failure dictionary storage and query helpers."
      contains: "structured lexicon entries"
    - path: ".planning/research/FAILURE-DICTIONARY.md"
      provides: "Living handbook seed aligned with implemented entry structure."
      contains: "entry shape, forbidden fixes, known-good guidance"
---

<objective>
Implement the first searchable failure dictionary / repair lexicon and connect it to operator debugging.

Output: structured lexicon entries queryable from the web workbench and future agents.
</objective>

<context>
@.planning/PROJECT.md
@.planning/research/FAILURE-DICTIONARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-operator-verification-failure-dictionary/07-CONTEXT.md
@.planning/phases/07-operator-verification-failure-dictionary/07-RESEARCH.md
@.planning/phases/07-operator-verification-failure-dictionary/07-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/database.py
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
@wxbot/bookkeeping-platform/bookkeeping_web/pages.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structured failure dictionary storage and indexing from repair history</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py, .planning/research/FAILURE-DICTIONARY.md</files>
  <action>Add an additive structured storage/query surface for failure dictionary entries. Seed it from deterministic repair-history patterns, approved replay fixtures, and explicit known rules already documented in `.planning/research/FAILURE-DICTIONARY.md`. Preserve fields such as failure code, symptom, trigger pattern, root cause, preferred scope, forbidden fixes, known-good fixes, replay fixture refs, test refs, related groups, frequency, and source case refs. Avoid copying raw message blobs into entries.</action>
  <acceptance_criteria>
Dictionary entries are searchable and structured.
Repeated failures aggregate into one handbook-style entry instead of duplicating logs.
Entries retain references to cases, fixtures, and tests.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend -v</automated>
  </verify>
  <done>The project now has a searchable repair handbook substrate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose lexicon lookup in the operator workbench</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/bookkeeping_web/pages.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <action>Add web/API support so operators can search the failure dictionary directly and, when viewing a message or repair case, jump to related dictionary entries. The UI should surface: symptom, root cause, preferred scope, do first, do not do, known-good fix, replay fixtures, and test refs. Keep the lexicon explicitly advisory: it is a repair handbook, not a publish override.</action>
  <acceptance_criteria>
Operators can search dictionary entries from the workbench.
The UI makes handbook guidance discoverable from a live case.
The dictionary does not grant mutation authority.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp tests.test_postgres_backend -v</automated>
  </verify>
  <done>Future agents and operators can now consult structured repair knowledge instead of depending on session memory.</done>
</task>

</tasks>

<threat_model>
- If the dictionary copies raw logs instead of structured guidance, it will become another context dump.
- If entries are not linked to cases/fixtures/tests, they will drift into folklore instead of verified knowledge.
- If handbook lookup is disconnected from live cases, operators will not use it during debugging.
</threat_model>

<verification>
Wave 3 is complete only when the project has a searchable failure dictionary connected to live cases and grounded in real repair history.
</verification>

<success_criteria>
The project no longer relies on long chat memory alone to repair known failure patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operator-verification-failure-dictionary/07-03-SUMMARY.md`
</output>
