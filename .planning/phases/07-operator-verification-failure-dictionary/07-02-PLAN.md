---
phase: 07-operator-verification-failure-dictionary
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_web/pages.py
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - OPS-01
  - EVID-02
must_haves:
  truths:
    - "Operators can inspect why rows were accepted, rejected, held, or untouched for one message without leaving the quotes workbench."
    - "Workbench wording remains proof-only and does not imply that inspection or confirmation itself changed the active wall."
    - "The workbench clarifies publish reasoning instead of hiding guarded no-op behavior."
  key_links:
    - from: "bookkeeping_web/pages.py"
      to: "bookkeeping_web/app.py"
      via: "interactive workbench reads the message-level evidence payload"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_web/pages.py"
      provides: "Operator verification workbench UI."
      contains: "candidate / validation / snapshot / publish reasoning presentation"
---

<objective>
Turn the message-level evidence payload into an operator-facing verification workbench inside the existing quotes page.

Output: a real UI for inspecting one message's candidate, validation, snapshot, repair, and publish reasoning.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/07-operator-verification-failure-dictionary/07-CONTEXT.md
@.planning/phases/07-operator-verification-failure-dictionary/07-RESEARCH.md
@.planning/phases/07-operator-verification-failure-dictionary/07-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_web/pages.py
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
@wxbot/bookkeeping-platform/tests/test_webapp.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a message-level verification panel to the quotes workbench</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/pages.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <action>Extend the quotes page so an operator can open one message and see: message metadata, grouped candidate rows, grouped validation outcomes (`publishable`, `held`, `rejected`), snapshot decision lineage, repair summary, and publish reasoning. Keep the UI scannable: group rows by outcome instead of dumping raw JSON by default. Make “untouched because delta/no-op/blocked” visible so the operator knows why a message did not change the wall.</action>
  <acceptance_criteria>
The workbench exposes one message's evidence in a human-debuggable way.
Row classes are clearly separated.
No wording implies that viewing evidence changed active facts.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp -v</automated>
  </verify>
  <done>The operator can inspect one message without leaving the quotes page.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock proof-only wording and publish reasoning clarity</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/pages.py, wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <action>Review the operator-facing wording in the new verification workbench and make it explicit that this surface is explanatory, not a publish side door. Add web tests that pin key phrases for: recorded snapshot decisions, no-op/untouched outcomes, blocked remediation, and guarded publish reasoning. Ensure the UI differentiates “validator rejected row” from “row remained untouched because the current publish mode did not authorize mutation.”</action>
  <acceptance_criteria>
The workbench language is proof-only and trustworthy.
Operators can distinguish rejection, hold, and untouched states.
No phrase suggests “already上墙” when the wall was unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp -v</automated>
  </verify>
  <done>The verification UI explains outcomes clearly without overstating authority.</done>
</task>

</tasks>

<threat_model>
- If the workbench is raw-JSON-heavy, operators will still need engineering help to debug.
- If proof wording is weak, operators may think confirmation or inspection already changed the wall.
- If untouched rows are not clearly explained, the UI will still fail `EVID-02`.
</threat_model>

<verification>
Wave 2 is complete only when operators can inspect one message end-to-end in the UI and the wording remains proof-only.
</verification>

<success_criteria>
The existing quotes page becomes a real debugging workbench instead of a thin exception list.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operator-verification-failure-dictionary/07-02-SUMMARY.md`
</output>
