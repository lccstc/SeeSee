---
phase: 07-operator-verification-failure-dictionary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/database.py
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - OPS-01
  - EVID-02
must_haves:
  truths:
    - "One quote document can be inspected through one coherent evidence payload instead of forcing operators to manually stitch together candidates, validation, repair, and snapshot state."
    - "Publish reasoning explains both mutation and non-mutation outcomes."
    - "Evidence APIs stay read-only and do not create new fact-mutation authority."
  key_links:
    - from: "bookkeeping_core/database.py"
      to: "bookkeeping_web/app.py"
      via: "message-level evidence payload assembled from durable candidate, validation, snapshot, repair, and publish semantics"
    - from: "quote_snapshot_decisions"
      to: "guarded publisher semantics"
      via: "resolved snapshot decisions explain why rows were or were not touched"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/database.py"
      provides: "Message-level verification evidence read helper(s)."
      contains: "quote document evidence aggregation"
    - path: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      provides: "Read-only evidence API endpoint(s)."
      contains: "operator verification payload"
---

<objective>
Create the message-level evidence substrate for operator verification without changing publish authority.

Output: one durable, read-only evidence payload keyed by `quote_document_id`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/FAILURE-DICTIONARY.md
@.planning/phases/07-operator-verification-failure-dictionary/07-CONTEXT.md
@.planning/phases/07-operator-verification-failure-dictionary/07-RESEARCH.md
@.planning/phases/07-operator-verification-failure-dictionary/07-VALIDATION.md
@AGENTS.md
@graphify-out/GRAPH_REPORT.md
@wxbot/bookkeeping-platform/bookkeeping_core/database.py
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build message-level verification evidence helpers in the database layer</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <action>Add additive read helpers keyed by `quote_document_id` that return a coherent evidence object containing: message metadata, candidate rows, latest validation run summary, grouped validation row results, snapshot decision, linked repair-case summary, and publish reasoning derived from guarded snapshot / validator semantics. The payload must explain why rows are publishable, held, rejected, or untouched. Keep the helper strictly read-only. Do not mutate `quote_price_rows` or repair state in this task.</action>
  <acceptance_criteria>
One message can be inspected through a single evidence payload.
The payload distinguishes `publishable`, `held`, `rejected`, and `untouched` outcomes explicitly.
No new write path is introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend -v</automated>
  </verify>
  <done>Database-layer evidence is coherent and grounded in existing durable lineage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose read-only operator evidence APIs with proof-oriented semantics</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <action>Add narrow read-only API route(s) for the message-level verification payload. The route should accept a `quote_document_id` (and optionally repair-case / exception indirection if already convenient) and return the evidence object from Task 1. Ensure the payload and route naming make it clear that this surface explains outcomes but does not publish, resolve, or mutate facts. Add web regressions that confirm proof-oriented semantics and make sure “untouched due to delta/no-op” is surfaced rather than lost.</action>
  <acceptance_criteria>
Operators can request one message-level evidence object through the web layer.
The API remains read-only and does not imply publication.
No bypass around validator or publisher is introduced.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp -v</automated>
  </verify>
  <done>One message can now be explained end-to-end through a read-only API.</done>
</task>

</tasks>

<threat_model>
- If evidence still requires joining several APIs mentally, the operator workbench will not materially improve debugging.
- If publish reasoning is not explicit, operators will continue to confuse parser failure with guarded no-op.
- If read-only routes accidentally gain side effects, Phase 07 would violate publisher custody.
</threat_model>

<verification>
Wave 1 is complete only when the codebase exposes a coherent, read-only message-level evidence payload grounded in durable lineage.
</verification>

<success_criteria>
The system can explain one message end-to-end without granting new mutation authority.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operator-verification-failure-dictionary/07-01-SUMMARY.md`
</output>
