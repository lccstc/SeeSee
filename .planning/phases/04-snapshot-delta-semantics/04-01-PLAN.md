---
phase: 04-snapshot-delta-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/quote_snapshot.py
  - wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
  - wxbot/bookkeeping-platform/bookkeeping_core/database.py
  - wxbot/bookkeeping-platform/sql/postgres_schema.sql
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_quote_validation.py
  - wxbot/bookkeeping-platform/tests/test_quote_exception_corpus.py
autonomous: true
requirements:
  - SNAP-01
  - OPS-02
must_haves:
  truths:
    - "Snapshot semantics are represented as a durable message-level decision surface rather than an ad hoc parser hint."
    - "The system can distinguish system hypothesis, unresolved state, and optional operator-confirmed decision with audit lineage."
    - "Gold corpus snapshot judgments are reusable regression material rather than disconnected notes."
  key_links:
    - from: "bookkeeping_core/quotes.py"
      to: "bookkeeping_core/quote_snapshot.py"
      via: "runtime produces snapshot hypotheses using a dedicated snapshot decision helper"
    - from: "bookkeeping_core/database.py"
      to: "sql/postgres_schema.sql"
      via: "durable snapshot decision storage and schema verification"
    - from: "tests.test_quote_exception_corpus"
      to: "tests.test_quote_validation"
      via: "real-sample message decisions and deterministic classification logic stay aligned"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/quote_snapshot.py"
      provides: "Dedicated snapshot decision contract and deterministic hypothesis logic."
      contains: "class or helpers for snapshot decision / hypothesis"
    - path: "wxbot/bookkeeping-platform/sql/postgres_schema.sql"
      provides: "Durable snapshot decision persistence."
      contains: "snapshot decision storage"
---

<objective>
Introduce a durable message-level snapshot decision model and deterministic hypothesis scaffolding, without granting automatic publish authority.

Output: persisted snapshot-decision lineage that the guarded publisher can safely consume in Wave 2.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-snapshot-delta-semantics/04-CONTEXT.md
@.planning/phases/04-snapshot-delta-semantics/04-RESEARCH.md
@.planning/phases/04-snapshot-delta-semantics/04-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py
@wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
@wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a dedicated snapshot decision contract and persistence surface</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quote_snapshot.py, wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/sql/postgres_schema.sql, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <action>Add a dedicated core module for snapshot semantics and an additive persistence surface linked to `quote_documents`. It must preserve at least: system hypothesis, hypothesis reason/evidence, current resolved decision, decision source (`system` or `operator`), optional confirmer identity, and confirmation timestamp. Keep `unresolved` explicit. Do not mutate `quote_price_rows` in this task. Update schema verification and backend tests so the new surface is durable and auditable.</action>
  <acceptance_criteria>
Snapshot semantics have a durable, message-level home separate from parser hints and validator publishability.
The system can persist unresolved hypotheses and later operator-confirmed decisions with audit lineage.
No active fact mutation occurs in this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_quote_validation -v</automated>
  </verify>
  <done>Snapshot decisions are now durable message-level objects rather than implied metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic hypothesis scaffolding tied to real corpus decisions</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quote_snapshot.py, wxbot/bookkeeping-platform/bookkeeping_core/quotes.py, wxbot/bookkeeping-platform/tests/test_quote_validation.py, wxbot/bookkeeping-platform/tests/test_quote_exception_corpus.py</files>
  <action>Teach runtime candidate capture to record a deterministic snapshot hypothesis instead of hard-coding `unresolved` for every message. Start narrow: explicit delta phrases, handoff/full-board patterns, and corpus-backed stable signals. When the evidence is not strong, keep the hypothesis unresolved. Add tests that pin this logic to real corpus judgments and prove unresolved is preserved when the system cannot justify a stronger claim.</action>
  <acceptance_criteria>
Runtime no longer stamps every message with the same placeholder snapshot hypothesis.
Deterministic cues are tied to explicit evidence and regressions, not freeform prompt logic.
Ambiguous messages remain unresolved instead of being upgraded optimistically.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_quote_validation tests.test_quote_exception_corpus -v</automated>
  </verify>
  <done>Snapshot hypotheses are now evidence-backed, auditable, and conservative.</done>
</task>

</tasks>

<threat_model>
- If snapshot decisions stay only in candidate metadata, the publisher will either re-infer dangerously or ignore them.
- If every message is still stamped `unresolved`, Phase 04 will fail to connect corpus knowledge to runtime semantics.
- If deterministic classification overreaches, the system could silently promote ambiguous deltas into full snapshots.
</threat_model>

<verification>
Wave 1 is complete only when snapshot semantics exist as durable message-level state and deterministic hypotheses remain conservative on ambiguous messages.
</verification>

<success_criteria>
The codebase now has a durable snapshot decision model with evidence-backed system hypotheses and optional operator-confirmed resolution.
</success_criteria>

<output>
After completion, create `.planning/phases/04-snapshot-delta-semantics/04-01-SUMMARY.md`
</output>
