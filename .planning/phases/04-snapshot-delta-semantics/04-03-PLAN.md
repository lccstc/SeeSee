---
phase: 04-snapshot-delta-semantics
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/bookkeeping_web/pages.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
autonomous: true
requirements:
  - OPS-02
  - SNAP-01
  - SNAP-02
  - SNAP-03
must_haves:
  truths:
    - "The operator can confirm or override snapshot semantics in v1 without bypassing the guarded publisher."
    - "Confirmation writes durable decision lineage but does not itself mutate active quote facts."
    - "Proof surfaces communicate snapshot decision state clearly and do not imply silent publication."
  key_links:
    - from: "bookkeeping_web/app.py"
      to: "bookkeeping_core/quote_snapshot.py"
      via: "minimal operator confirmation endpoint writes durable snapshot decisions"
    - from: "bookkeeping_web/pages.py"
      to: "bookkeeping_web/app.py"
      via: "UI exposes snapshot hypothesis / confirmed decision state for one message"
    - from: "tests.test_webapp"
      to: "tests.test_postgres_backend"
      via: "operator confirmation is proven durable and fact-neutral"
---

<objective>
Add the minimal v1 human confirmation flow for snapshot semantics, without turning confirmation into a new publish side door.

Output: one narrow operator path that records confirmed `full_snapshot` / `delta_update` decisions for a message and exposes proof-oriented wording in the web UI.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-snapshot-delta-semantics/04-CONTEXT.md
@.planning/phases/04-snapshot-delta-semantics/04-RESEARCH.md
@.planning/phases/04-snapshot-delta-semantics/04-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
@wxbot/bookkeeping-platform/bookkeeping_web/pages.py
@wxbot/bookkeeping-platform/tests/test_webapp.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a minimal snapshot confirmation flow that records decisions but does not publish</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/bookkeeping_web/pages.py, wxbot/bookkeeping-platform/tests/test_webapp.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <action>Add one narrow operator surface for a message-level snapshot decision: show current system hypothesis, show any current resolved decision, and allow an authenticated operator to confirm or override to `full_snapshot` / `delta_update`. The endpoint must persist decision lineage only. It must not mutate `quote_price_rows` or silently invoke the guarded publisher as a side effect.</action>
  <acceptance_criteria>
The operator can record a snapshot decision for one message in v1.
The decision persists who/when/what changed.
No active quote facts change merely because a decision was confirmed.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp tests.test_postgres_backend -v</automated>
  </verify>
  <done>v1 now has a durable human confirmation gate for snapshot semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make snapshot decision wording and proof flows explicit</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/pages.py, wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/tests/test_webapp.py, wxbot/bookkeeping-platform/tests/test_runtime.py</files>
  <action>Update relevant proof/result surfaces so the operator can clearly tell: system hypothesis, whether a human-confirmed decision exists, and whether active facts were changed or not. Preserve the repo’s proof-only wording discipline (`未改动报价墙`) for decision recording and validation-first flows. Add regressions that fail if wording drifts into implying silent publish.</action>
  <acceptance_criteria>
The UI distinguishes “decision recorded” from “facts published”.
The operator can see whether a message is unresolved, delta-confirmed, or full-confirmed.
Proof wording remains explicit and non-misleading.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp tests.test_runtime -v</automated>
  </verify>
  <done>Snapshot semantics are now visible and understandable without widening publish authority.</done>
</task>

</tasks>

<threat_model>
- If the confirmation route directly invokes the guarded publisher, it recreates the exact kind of side door this project is trying to avoid.
- If the UI says “已处理/已生效” when only a decision was recorded, operators will trust the wrong thing.
- If confirmation lineage is not durable, later replay/publish/debug work will be impossible to audit.
</threat_model>

<verification>
Wave 3 is complete only when the operator can confirm snapshot semantics safely and the UI clearly separates “decision recorded” from “facts published”.
</verification>

<success_criteria>
v1 now has a narrow, auditable human confirmation gate for snapshot semantics that remains proof-oriented and fact-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/04-snapshot-delta-semantics/04-03-SUMMARY.md`
</output>
