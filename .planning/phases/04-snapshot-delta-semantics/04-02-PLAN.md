---
phase: 04-snapshot-delta-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py
  - wxbot/bookkeeping-platform/bookkeeping_core/database.py
  - wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
autonomous: true
requirements:
  - SNAP-02
  - SNAP-03
  - FACT-01
must_haves:
  truths:
    - "Unresolved snapshot decisions behave as delta-safe publishes instead of destructive replacement."
    - "Only confirmed full snapshots may inactivate previously active rows absent from the current message."
    - "Snapshot-aware publish behavior keeps Phase 03 transaction, rollback, and lock guarantees."
  key_links:
    - from: "bookkeeping_core/quote_snapshot.py"
      to: "bookkeeping_core/quote_publisher.py"
      via: "guarded publisher consumes resolved snapshot decisions rather than inventing semantics internally"
    - from: "bookkeeping_core/quote_publisher.py"
      to: "bookkeeping_core/database.py"
      via: "delta-safe upsert and confirmed-full inactivation run inside the existing guarded transaction"
    - from: "tests.test_postgres_backend"
      to: "tests.test_runtime"
      via: "publisher semantics are proven both in PG mutation behavior and runtime entrypoint behavior"
---

<objective>
Wire snapshot decisions into the guarded publisher so unresolved/default messages behave as delta updates and only confirmed full snapshots may inactivate unseen prior SKUs.

Output: snapshot-aware guarded publish semantics without re-opening any side doors around Phase 03 custody.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-snapshot-delta-semantics/04-CONTEXT.md
@.planning/phases/04-snapshot-delta-semantics/04-RESEARCH.md
@.planning/phases/04-snapshot-delta-semantics/04-VALIDATION.md
@.planning/phases/03-fact-protection-publisher/03-VERIFICATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py
@wxbot/bookkeeping-platform/bookkeeping_core/database.py
@wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit delta-safe and confirmed-full publish semantics to the guarded publisher</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py, wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <action>Replace the current replacement-style assumption with explicit snapshot-aware publish behavior. The guarded publisher must consume the resolved snapshot decision for a quote document and choose one of two mutation paths: delta-safe upsert-only, or confirmed-full inactivation + apply. Keep unresolved/default behavior on the delta-safe path. Preserve the existing transaction, advisory lock, and rollback guarantees.</action>
  <acceptance_criteria>
The publisher never inactivates unseen rows for unresolved or delta decisions.
Only confirmed full snapshots are allowed to deactivate unseen active rows.
Failure at any step still rolls back cleanly and leaves prior active facts untouched.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime -v</automated>
  </verify>
  <done>Snapshot-aware guarded publish semantics now exist and remain fact-safe by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep runtime validation-first while exposing proposed snapshot-aware publish semantics</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quotes.py, wxbot/bookkeeping-platform/tests/test_runtime.py</files>
  <action>Update runtime so validation-first capture can surface the resolved snapshot decision and proposed guarded publish mode without silently taking production authority. Runtime must continue defaulting to fact-neutral validation mode unless an explicit guarded publish path is invoked elsewhere. Add runtime regressions that prove unresolved defaults remain delta-safe and that full-snapshot semantics are visible but not silently auto-applied in the validation-first path.</action>
  <acceptance_criteria>
Runtime remains validation-first after Phase 04.
The runtime result payload exposes enough snapshot/publish-mode information for later operator/debug flows.
There is no hidden switch from validation-only into automatic production publication.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_runtime tests.test_postgres_backend -v</automated>
  </verify>
  <done>Publisher semantics are ready without violating the project’s validation-first posture.</done>
</task>

</tasks>

<threat_model>
- If unresolved messages still route to replacement-style publish, Phase 04 will violate the project’s hardest safety rule.
- If confirmed full snapshots do not deactivate unseen rows, the system cannot express true handoff/full-board semantics.
- If runtime quietly starts applying snapshot-aware publishes by default, v1 will overstep its validation-first scope.
</threat_model>

<verification>
Wave 2 is complete only when delta-safe default behavior and confirmed-full inactivation are both explicit, tested, and still owned by the guarded publisher.
</verification>

<success_criteria>
The guarded publisher now implements safe snapshot semantics: unresolved defaults to delta, and only confirmed full snapshots may inactivate unseen prior SKUs.
</success_criteria>

<output>
After completion, create `.planning/phases/04-snapshot-delta-semantics/04-02-SUMMARY.md`
</output>
