---
phase: 08-experimental-active-wall-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
  - wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - GOV-01
must_haves:
  truths:
    - "Experimental mode performs real wall mutation only through guarded publisher custody."
    - "Experimental mode keeps downstream actions off."
    - "Experimental mode does not weaken validator or snapshot semantics."
  key_links:
    - from: "bookkeeping_core/quotes.py"
      to: "quote_publisher.py"
      via: "runtime publish path respects explicit experimental wall mode"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/quotes.py"
      provides: "experimental wall execution switch"
      contains: "publisher-only real wall mutation gating"
---

<objective>
Enable a real operator-owned experimental wall mode while keeping all wall mutation behind guarded publisher custody and keeping downstream actions disabled.

Output: a deterministic runtime mode that allows real wall updates without formal production takeover.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@PROJECT/P8单人运营实验墙上线标准.md
@.planning/phases/08-experimental-active-wall-gate/08-CONTEXT.md
@.planning/phases/08-experimental-active-wall-gate/08-RESEARCH.md
@.planning/phases/08-experimental-active-wall-gate/08-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
@wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit experimental-wall mode and keep all quote mutation inside guarded publisher custody</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quotes.py, wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py, wxbot/bookkeeping-platform/tests/test_runtime.py</files>
  <action>Add an explicit runtime mode that enables real wall mutation for the operator-owned experimental wall. The mode must still require validator-owned `publishable_rows`, guarded publisher execution, and snapshot-safe semantics. No alternate write path may be introduced.</action>
  <acceptance_criteria>
Experimental mode performs real wall mutation.
Mutation still happens only through guarded publisher custody.
Zero publishable rows and unresolved snapshot rules remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime -v</automated>
  </verify>
  <done>Experimental wall mode is real, but still safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep downstream actions disabled in experimental wall mode</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quotes.py, wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/tests/test_runtime.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <action>Audit the quote-wall-adjacent runtime and web flows to ensure experimental wall mode does not trigger settlement, notification, external send, or other downstream automation. Expose the mode state read-only if needed, but do not add new operator powers here.</action>
  <acceptance_criteria>
Experimental wall mode updates only the wall.
No downstream business side effects are triggered.
Operators can inspect the mode state without gaining new authority.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_runtime tests.test_webapp -v</automated>
  </verify>
  <done>The wall can update experimentally without silently spreading business authority.</done>
</task>

</tasks>

<threat_model>
- If experimental mode uses any path other than guarded publisher, Phase 08 breaks custody.
- If downstream actions remain latent, operators may unknowingly treat the wall as formal production.
- If runtime mode is implicit, later debugging will not know whether a wall mutation came from experiment or formal operation.
</threat_model>

<verification>
Wave 1 is complete only when the wall can mutate experimentally through guarded publisher custody and no downstream action is enabled.
</verification>

<output>
After completion, create `.planning/phases/08-experimental-active-wall-gate/08-01-SUMMARY.md`
</output>
