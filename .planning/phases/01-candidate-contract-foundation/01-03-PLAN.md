---
phase: 01-candidate-contract-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/bookkeeping_web/pages.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - EVID-01
  - CAND-01
  - CAND-02
must_haves:
  truths:
    - "Replay and harvest-save persist a new replay candidate run instead of refreshing the active quote board."
    - "Replay responses identify the new candidate run and explicitly state that active facts were not mutated."
    - "Operator-facing harvest/replay copy no longer claims that save or replay published rows to the board."
    - "Web regressions no longer treat harvest/replay success as evidence that `/api/quotes/board` changed."
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      provides: "Candidate-only replay helper and harvest-save replay wiring."
      contains: "_replay_latest_quote_document_with_current_template"
    - path: "wxbot/bookkeeping-platform/bookkeeping_web/pages.py"
      provides: "Harvest/replay UI copy and client-side status text aligned to candidate replay with no board-mutation claims."
      contains: "quoteExceptionHarvestStatus"
    - path: "wxbot/bookkeeping-platform/tests/test_webapp.py"
      provides: "Rebased harvest/replay web regressions asserting candidate runs instead of board refresh."
      contains: "test_quote_exception_harvest_preview_and_save"
  key_links:
    - from: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_core/quotes.py"
      via: "parse_quote_message_to_candidate(..., run_kind='replay', replay_of_quote_document_id=...)"
      pattern: "run_kind=['\"]replay['\"]"
    - from: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_core/database.py"
      via: "record_quote_candidate_bundle(candidate=...)"
      pattern: "record_quote_candidate_bundle"
    - from: "wxbot/bookkeeping-platform/bookkeeping_web/pages.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      via: "harvest modal summary and alert text consume replay payload fields like `replayed`, `rows`, and `mutated_active_facts`"
      pattern: "mutated_active_facts|quote-harvest-summary|已上墙|候选"
    - from: "wxbot/bookkeeping-platform/tests/test_webapp.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      via: "harvest-save response assertions on replay metadata and board non-mutation"
      pattern: "mutated_active_facts|replay_of_quote_document_id"
    - from: "wxbot/bookkeeping-platform/tests/test_webapp.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_web/pages.py"
      via: "HTML copy assertions proving the page no longer says replay/save published rows"
      pattern: "继续补一段并上墙|已上墙|候选重放|未改动报价墙"
---

<objective>
Move replay and harvest-save onto the same candidate-only custody boundary as runtime capture, and explicitly rebaseline the web regressions that still expect board mutation.

Purpose: Enforce D-05 and D-06 for replay paths, satisfy the phase-specific replay constraint, and prevent Phase 1 execution from drifting into guarded-publisher work or misleading operator copy.
Output: Candidate-only replay behavior in `app.py`, operator-facing copy updates in `pages.py`, and web tests that prove harvest/replay no longer refresh the active board.
</objective>

<execution_context>
@/Users/newlcc/.codex/get-shit-done/workflows/execute-plan.md
@/Users/newlcc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-candidate-contract-foundation/01-CONTEXT.md
@.planning/phases/01-candidate-contract-foundation/01-RESEARCH.md
@.planning/phases/01-candidate-contract-foundation/01-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py
@wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
@wxbot/bookkeeping-platform/bookkeeping_web/pages.py
@wxbot/bookkeeping-platform/tests/test_webapp.py

<interfaces>
Current replay helper in `wxbot/bookkeeping-platform/bookkeeping_web/app.py`:

```python
def _replay_latest_quote_document_with_current_template(
    db: BookkeepingDB,
    *,
    exc_row: dict,
    record_exceptions: bool = True,
) -> dict: ...
```

Current harvest-save flow only calls replay when `preview["is_latest_for_group"]` and the exception is fully resolved or already resolved. Preserve that gate, but change replay output to create a new candidate run, keep `quote_price_rows` untouched, and update `pages.py` copy so the operator sees “candidate replay / no board mutation” rather than “published rows.”
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert replay and harvest-save to persist replay candidate runs only</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/app.py</files>
  <read_first>
.planning/phases/01-candidate-contract-foundation/01-CONTEXT.md
.planning/phases/01-candidate-contract-foundation/01-RESEARCH.md
wxbot/bookkeeping-platform/bookkeeping_web/app.py
wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py
wxbot/bookkeeping-platform/bookkeeping_core/database.py
  </read_first>
  <action>Rewrite `_replay_latest_quote_document_with_current_template(...)` so it reuses the Phase 1 helper from `quotes.py` exactly as defined in Plan `01-02` to build a `QuoteCandidateMessage` with `run_kind=\"replay\"` and `replay_of_quote_document_id=<original quote_document_id>`, persists that bundle through `db.record_quote_candidate_bundle(candidate=...)`, and records replay exceptions without calling `deactivate_old_quotes_for_group(...)` or `upsert_quote_price_row_with_history(...)`. Do not reopen `quotes.py` in this plan unless Plan `01-02` failed to land the agreed helper contract; this plan should consume the existing replay-capable helper, not fork it. Keep the existing `replayed`, `rows`, `exceptions`, `quote_document_id`, and `remaining_lines` keys in the replay payload for brownfield compatibility, but make `rows` mean persisted replay candidate row count and add `mutated_active_facts: False`. Leave `/api/quotes/board` on `quote_price_rows`; do not backfill candidate rows into board output in this phase, and do not add a new HTTP candidate inspection surface. Preserve the current harvest-save gate that only attempts replay when the harvested exception is latest-for-group and resolved, but ensure that replay now only creates the new replay candidate document.</action>
  <acceptance_criteria>
`wxbot/bookkeeping-platform/bookkeeping_web/app.py` replay helper contains `run_kind=\"replay\"`.
`wxbot/bookkeeping-platform/bookkeeping_web/app.py` replay helper contains `replay_of_quote_document_id`.
`wxbot/bookkeeping-platform/bookkeeping_web/app.py` replay payload contains `mutated_active_facts`.
`rg -n "deactivate_old_quotes_for_group|upsert_quote_price_row_with_history" wxbot/bookkeeping-platform/bookkeeping_web/app.py` returns no matches inside the replay helper path.
`/api/quotes/board` code still reads from `list_quote_board`, not candidate rows.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp -v</automated>
  </verify>
  <done>Replay/harvest-save creates a new candidate run with replay lineage and explicit non-mutation metadata, while the active quote board remains fact-table driven.</done>
</task>

<task type="auto">
  <name>Task 2: Rebaseline UI copy and web regressions from board refresh to replay-candidate evidence</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/pages.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <read_first>
.planning/phases/01-candidate-contract-foundation/01-VALIDATION.md
wxbot/bookkeeping-platform/bookkeeping_web/pages.py
wxbot/bookkeeping-platform/tests/test_webapp.py
wxbot/bookkeeping-platform/bookkeeping_web/app.py
wxbot/bookkeeping-platform/bookkeeping_core/database.py
  </read_first>
  <action>Update `bookkeeping_web/pages.py` so the harvest modal summary, save button labels, success alert, and `quoteExceptionHarvestStatus(...)` no longer claim replay/save “已上墙” or “并上墙”. Replace that copy with candidate-language that makes the Phase 1 boundary explicit, for example “已生成候选重放” / “未改动报价墙” while still preserving operator guidance about latest-message gating and remaining sections. Then update the current harvest/replay tests that still assert board mutation after save. Specifically rebaseline `test_quote_exception_harvest_preview_and_save`, `test_quote_exception_harvest_multi_section_final_save_replays_board`, and `test_quote_exception_harvest_save_can_append_missing_section_after_resolved` so they assert the new replay candidate behavior instead of `/api/quotes/board` growth. Rename the two board-oriented test names so the names match their new semantics. In each rebased test, assert `saved["replay"]["replayed"]` as before, assert `saved["replay"]["mutated_active_facts"] is False`, assert the returned replay `quote_document_id` differs from the original exception document, assert the new replay `quote_documents` row has `run_kind=\"replay\"` and `replay_of_quote_document_id=<original>`, assert `quote_candidate_rows` count matches the harvested rows for that replay document, and assert `/api/quotes/board` remains empty unless the test explicitly seeded fact rows before harvest. Add at least one page-render assertion that the HTML/JS payload no longer contains `继续补一段并上墙` or the replay success `已上墙` copy and now contains the replacement candidate wording. Keep the strict replay validation gate assertions intact; only rebaseline the post-save expectations and UI copy that previously treated replay as a board refresh.</action>
  <acceptance_criteria>
`wxbot/bookkeeping-platform/bookkeeping_web/pages.py` no longer contains harvest/replay success copy with `已上墙` or `继续补一段并上墙`.
`wxbot/bookkeeping-platform/bookkeeping_web/pages.py` contains replacement candidate-language copy describing replay without board mutation.
`wxbot/bookkeeping-platform/tests/test_webapp.py` contains `mutated_active_facts`.
`wxbot/bookkeeping-platform/tests/test_webapp.py` contains `replay_of_quote_document_id`.
The rebased harvest/replay tests assert that `/api/quotes/board` remains unchanged or empty after replay when no fact rows were seeded.
The old board-refresh expectations `self.assertEqual(len(board["rows"]), 2)`, `4`, and `3` are removed from the rebased harvest/replay tests.
The web tests include at least one page-render assertion covering the updated candidate-only copy.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp -v</automated>
  </verify>
  <done>Web regressions and operator-facing copy now prove that replay success means “new candidate evidence exists” rather than “the active board changed,” which preserves the Phase 1 custody boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exception replay UI -> replay helper | Operator-triggered replay can currently mutate active facts through the web layer. |
| harvest-save route -> quote board facts | Template-save success previously implied a board refresh, which is too much authority for Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | `_replay_latest_quote_document_with_current_template` | mitigate | Persist a replay candidate run with `run_kind='replay'` and `replay_of_quote_document_id`, and remove all replay-path fact mutations. |
| T-01-08 | E | Harvest-save route authority | mitigate | Keep harvest-save limited to template/config changes plus replay candidate persistence; do not let it refresh `/api/quotes/board` in Phase 1. |
| T-01-09 | R | Replay evidence traceability | mitigate | Return the new replay `quote_document_id`, preserve `remaining_lines`, and assert replay lineage in web tests. |
</threat_model>

<verification>
Run `tests.test_webapp` against PostgreSQL after the replay refactor. The plan is incomplete if harvest/replay assertions are updated without preserving DB-backed verification of the new replay candidate documents.
</verification>

<success_criteria>
Replay and harvest-save now create replay candidate evidence that can be inspected and compared later, while `quote_price_rows` and `/api/quotes/board` remain unchanged until guarded publisher work in later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-candidate-contract-foundation/01-03-SUMMARY.md`
</output>
