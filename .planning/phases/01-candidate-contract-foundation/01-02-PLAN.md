---
phase: 01-candidate-contract-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
autonomous: true
requirements:
  - CAND-01
  - CAND-02
must_haves:
  truths:
    - "Runtime quote capture persists a candidate message and row candidates instead of mutating active quote facts."
    - "Candidate rows preserve parser lineage, message fingerprint, and per-row evidence during runtime ingestion."
    - "Rejected or non-publishable rows still leave evidence in the exception flow without deactivating existing board facts."
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/quotes.py"
      provides: "Candidate-building runtime capture path and shared parse-to-candidate helpers."
      contains: "parse_quote_message_to_candidate"
    - path: "wxbot/bookkeeping-platform/tests/test_runtime.py"
      provides: "Runtime regressions proving candidate-only capture and no fact mutation."
      contains: "test_runtime_quote_capture_persists_candidate_bundle_without_mutating_quote_price_rows"
  key_links:
    - from: "wxbot/bookkeeping-platform/bookkeeping_core/quotes.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_core/database.py"
      via: "record_quote_candidate_bundle(candidate)"
      pattern: "record_quote_candidate_bundle\\(candidate"
    - from: "wxbot/bookkeeping-platform/bookkeeping_core/runtime.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_core/quotes.py"
      via: "UnifiedBookkeepingRuntime.quote_capture.capture_from_message"
      pattern: "capture_from_message"
    - from: "wxbot/bookkeeping-platform/tests/test_runtime.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_core/quotes.py"
      via: "process_envelope-driven candidate persistence assertions"
      pattern: "quote_candidate_rows|run_kind"
---

<objective>
Route the runtime parsing path through the Phase 1 candidate contract and remove parser-direct fact writes.

Purpose: Enforce D-04 to D-06 during live message capture while preserving exception evidence and keeping `quote_price_rows` as a later-phase fact table.
Output: Shared parse-to-candidate helpers in `quotes.py` and runtime regressions proving candidate-only persistence.
</objective>

<execution_context>
@/Users/newlcc/.codex/get-shit-done/workflows/execute-plan.md
@/Users/newlcc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-candidate-contract-foundation/01-CONTEXT.md
@.planning/phases/01-candidate-contract-foundation/01-RESEARCH.md
@.planning/phases/01-candidate-contract-foundation/01-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py
@wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
@wxbot/bookkeeping-platform/bookkeeping_core/runtime.py
@wxbot/bookkeeping-platform/tests/test_runtime.py

<interfaces>
From `wxbot/bookkeeping-platform/bookkeeping_core/quotes.py` today:

```python
class QuoteCaptureService:
    def capture_from_message(
        self,
        envelope: NormalizedMessageEnvelope,
        *,
        raw_text: str | None = None,
    ) -> dict[str, Any]: ...
```

From Plan `01-01`, the runtime path should now consume:

```python
class QuoteCandidateMessage: ...
class QuoteCandidateRow: ...
def record_quote_candidate_bundle(self, *, candidate: QuoteCandidateMessage) -> int: ...
```

This plan must make runtime capture build that contract and stop calling the current fact-mutation methods from `quotes.py`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor quote capture to build and persist runtime candidates only</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quotes.py</files>
  <read_first>
.planning/phases/01-candidate-contract-foundation/01-CONTEXT.md
.planning/phases/01-candidate-contract-foundation/01-RESEARCH.md
wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py
wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
wxbot/bookkeeping-platform/bookkeeping_core/runtime.py
wxbot/bookkeeping-platform/bookkeeping_core/database.py
  </read_first>
  <action>In `quotes.py`, add a shared helper named `parse_quote_message_to_candidate(...) -> QuoteCandidateMessage` plus row-mapping helpers that convert both missing-template outcomes and template-parser outcomes into the Phase 1 contract. Compute `message_fingerprint` with a deterministic SHA-256 over the message identity and raw text, set `snapshot_hypothesis=\"unresolved\"`, set `snapshot_hypothesis_reason=\"phase1-default\"`, set `run_kind=\"runtime\"`, and populate `parser_kind` with the actual path used (`group-parser`, `strict-section`, or equivalent existing parser identifier). Build each `normalized_sku_key` from normalized `card_type`, `country_or_currency`, `amount_range`, `multiplier`, and `form_factor`. Set `row_publishable` only from the current prevalidation rule `quote_status == \"active\" and confidence >= AUTO_PUBLISH_CONFIDENCE`, and set `publishability_basis=\"parser_prevalidation\"`. Change `QuoteCaptureService.capture_from_message(...)` to persist candidates through `self.db.record_quote_candidate_bundle(candidate=...)`, continue recording parsed exceptions and low-confidence rows into `quote_parse_exceptions`, and remove all runtime calls to `deactivate_old_quotes_for_group(...)` and `upsert_quote_price_row_with_history(...)` from `quotes.py`.</action>
  <acceptance_criteria>
`wxbot/bookkeeping-platform/bookkeeping_core/quotes.py` contains `def parse_quote_message_to_candidate(`.
`wxbot/bookkeeping-platform/bookkeeping_core/quotes.py` contains `record_quote_candidate_bundle(candidate=`.
`wxbot/bookkeeping-platform/bookkeeping_core/quotes.py` contains `snapshot_hypothesis=\"unresolved\"` and `snapshot_hypothesis_reason=\"phase1-default\"`.
`wxbot/bookkeeping-platform/bookkeeping_core/quotes.py` contains `publishability_basis=\"parser_prevalidation\"`.
`rg -n "deactivate_old_quotes_for_group|upsert_quote_price_row_with_history" wxbot/bookkeeping-platform/bookkeeping_core/quotes.py` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_runtime -v</automated>
  </verify>
  <done>Runtime quote capture now persists candidate documents and rows, preserves exception evidence, and no longer mutates `quote_price_rows` directly.</done>
</task>

<task type="auto">
  <name>Task 2: Rebaseline runtime tests around candidate-only ingestion</name>
  <files>wxbot/bookkeeping-platform/tests/test_runtime.py</files>
  <read_first>
.planning/phases/01-candidate-contract-foundation/01-VALIDATION.md
wxbot/bookkeeping-platform/tests/test_runtime.py
wxbot/bookkeeping-platform/bookkeeping_core/runtime.py
wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
wxbot/bookkeeping-platform/bookkeeping_core/database.py
  </read_first>
  <action>Add focused PostgreSQL-backed runtime tests named `test_runtime_quote_capture_persists_candidate_bundle_without_mutating_quote_price_rows` and `test_runtime_quote_capture_records_missing_template_candidate_header`. Drive them through `UnifiedBookkeepingRuntime.process_envelope(...)`, not by calling DB methods directly. Assert the stored `quote_documents` row includes `run_kind=\"runtime\"`, non-empty `message_fingerprint`, `snapshot_hypothesis=\"unresolved\"`, parser lineage fields, and that `list_quote_candidate_rows(...)` returns the expected row count and `source_line` values. Assert `quote_price_rows` remains empty for the runtime message and that parsed failures still create `quote_parse_exceptions`. If `BOOKKEEPING_TEST_DSN` is unavailable, stop and report the task blocked instead of dropping the runtime verification.</action>
  <acceptance_criteria>
`wxbot/bookkeeping-platform/tests/test_runtime.py` contains `test_runtime_quote_capture_persists_candidate_bundle_without_mutating_quote_price_rows`.
`wxbot/bookkeeping-platform/tests/test_runtime.py` contains `test_runtime_quote_capture_records_missing_template_candidate_header`.
The runtime tests assert `run_kind`, `message_fingerprint`, and `snapshot_hypothesis`.
The runtime tests assert that `quote_price_rows` count is `0` for candidate-only capture.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_runtime -v</automated>
  </verify>
  <done>Runtime regressions prove the candidate boundary is enforced in the live ingestion path and that existing active facts are not touched by Phase 1 capture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| normalized inbound message -> quote capture | Untrusted chat input becomes candidate evidence here. |
| runtime parser output -> DB fact tables | This is the brownfield bypass path that previously mutated active quote facts directly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `QuoteCaptureService.capture_from_message` | mitigate | Replace runtime `deactivate_old_quotes_for_group` and `upsert_quote_price_row_with_history` calls with `record_quote_candidate_bundle(candidate=...)`. |
| T-01-05 | R | Candidate lineage during runtime capture | mitigate | Populate `parser_kind`, `parser_template`, `parser_version`, `message_fingerprint`, `snapshot_hypothesis`, and per-row evidence fields in the runtime helper before persistence. |
| T-01-06 | D | Loss of exception evidence when active writes are removed | mitigate | Keep `record_quote_exception_unless_suppressed(...)` for parsed exceptions and non-publishable rows so failures still enter the exception loop. |
</threat_model>

<verification>
Run `tests.test_runtime` against PostgreSQL after the runtime refactor. Runtime verification is incomplete if the suite passes only by bypassing candidate persistence or by omitting the DB-backed command.
</verification>

<success_criteria>
Live runtime ingestion now follows `raw input -> candidate contract -> exception evidence`, with no direct writes into `quote_price_rows` and with runtime tests proving the active board stays untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/01-candidate-contract-foundation/01-02-SUMMARY.md`
</output>
