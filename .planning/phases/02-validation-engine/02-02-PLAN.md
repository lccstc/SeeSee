---
phase: 02-validation-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/quote_validation.py
  - wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/tests/test_quote_validation.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - VALI-01
must_haves:
  truths:
    - "Every persisted candidate bundle can be validated through a deterministic schema-validation pipeline."
    - "Runtime and replay automatically create validation runs after candidate persistence."
    - "Messages with zero candidate rows still produce an explicit no-publish validation result rather than falling through implicitly."
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/quote_validation.py"
      provides: "Schema-validation engine and execution entrypoints."
      contains: "def validate_quote_candidate_document("
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/quotes.py"
      provides: "Runtime integration that validates new candidate bundles."
      contains: "record_quote_validation_run"
    - path: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      provides: "Replay integration that validates replay candidate bundles."
      contains: "validation_run_id"
    - path: "wxbot/bookkeeping-platform/tests/test_quote_validation.py"
      provides: "Focused validator logic tests."
      contains: "test_validate_quote_candidate_document_rejects_schema_failures"
---

<objective>
Implement the formal schema validator and wire it into runtime and replay so candidate persistence is immediately followed by durable validation.

Output: validator execution logic, automatic validation wiring for runtime and replay, and regression coverage proving persisted candidates always receive a validation result.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-validation-engine/02-RESEARCH.md
@.planning/phases/02-validation-engine/02-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py
@wxbot/bookkeeping-platform/bookkeeping_core/quote_validation.py
@wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement deterministic schema validation for candidate documents and rows</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quote_validation.py, wxbot/bookkeeping-platform/tests/test_quote_validation.py</files>
  <action>Implement a validator entrypoint that accepts a candidate document plus its candidate rows and returns a persisted-ready validation run. Schema validation must check required normalized fields, non-empty `normalized_sku_key`, valid source-line evidence, and a numeric positive `price`. Rows that fail schema validation must be rejected with stable structured reason codes. A message with zero candidate rows must still produce a message-level validation result with `publishable_row_count = 0` and an explicit no-publish decision.</action>
  <acceptance_criteria>
`quote_validation.py` contains `validate_quote_candidate_document`.
Schema-failure reason codes are stable and machine-readable.
Zero-row candidate documents produce explicit validation results.
`tests/test_quote_validation.py` covers schema pass/fail behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_quote_validation -v</automated>
  </verify>
  <done>Schema validation can deterministically classify parser output before any later publish step exists.</done>
</task>

<task type="auto">
  <name>Task 2: Run validation automatically in runtime and replay paths</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quotes.py, wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/tests/test_runtime.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <action>After candidate persistence in runtime and replay, execute the validator and persist one validation run plus row decisions. Runtime and replay should return or expose enough metadata for tests to prove the validation run exists, but this plan must not add a new operator workbench or publisher path. Keep active quote facts untouched. Add tests proving runtime-generated and replay-generated candidate documents each receive validation runs automatically.</action>
  <acceptance_criteria>
Runtime candidate capture persists a validation run automatically.
Replay candidate capture persists a validation run automatically.
No active quote facts are mutated by validator execution.
`tests.test_runtime` and `tests.test_webapp` assert validation-run creation.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_quote_validation tests.test_runtime tests.test_webapp -v</automated>
  </verify>
  <done>Every candidate document produced by runtime or replay now receives a formal validator verdict immediately after persistence.</done>
</task>

</tasks>

<threat_model>
- If runtime and replay do not validate through the same entrypoint, later comparisons become unreliable.
- If zero-row messages produce no validator result, publisher work will have to infer “no publish” from absence, which is unsafe.
- If validator execution mutates facts, Phase 02 would violate Phase 01 custody guarantees.
</threat_model>

<verification>
This plan is incomplete unless validator unit tests plus runtime/replay PostgreSQL-backed regressions pass.
</verification>

<success_criteria>
The system automatically creates durable schema-validation results for every candidate document produced in runtime and replay flows.
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-engine/02-02-SUMMARY.md`
</output>

