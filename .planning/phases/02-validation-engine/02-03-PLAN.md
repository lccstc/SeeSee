---
phase: 02-validation-engine
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/quote_validation.py
  - wxbot/bookkeeping-platform/bookkeeping_core/database.py
  - wxbot/bookkeeping-platform/tests/test_quote_validation.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - VALI-02
  - VALI-03
must_haves:
  truths:
    - "A single message can yield publishable, rejected, and held rows without ambiguity."
    - "Business-rule outcomes are durable, structured, and separate from parser-side hints."
    - "Later publisher work can read `publishable_rows` from validator outputs without re-deriving them ad hoc."
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/quote_validation.py"
      provides: "Business-rule validation and final decision aggregation."
      contains: "def separate_publishable_rows("
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/database.py"
      provides: "Helpers for latest validation result and publishable row lookup."
      contains: "def list_publishable_quote_candidate_rows("
    - path: "wxbot/bookkeeping-platform/tests/test_quote_validation.py"
      provides: "Mixed-outcome validator coverage."
      contains: "test_validate_quote_candidate_document_supports_publishable_rejected_and_held_rows"
---

<objective>
Complete the validator by adding business-rule evaluation and an explicit `publishable_rows` surface that later guarded publisher work can trust.

Output: formal row decisions for publishable/rejected/held, durable mixed-outcome summaries, and query helpers that expose publishable rows from the latest validation run.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-validation-engine/02-RESEARCH.md
@.planning/phases/02-validation-engine/02-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/quote_validation.py
@wxbot/bookkeeping-platform/bookkeeping_core/database.py
@wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add business-rule validation and final decision taxonomy</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quote_validation.py, wxbot/bookkeeping-platform/tests/test_quote_validation.py</files>
  <action>Extend the validator with explicit business-rule evaluation. At minimum: `quote_status != active` must reject, low confidence must hold, partial normalization must hold, ambiguous restriction parsing must hold, and duplicate `normalized_sku_key` rows within the same message must hold until later human or rule clarification. Preserve parser-side hints as upstream evidence but make validator decisions authoritative. Add focused tests proving one message can produce publishable, rejected, and held rows together.</action>
  <acceptance_criteria>
Validator supports mixed message outcomes.
Business-rule reason codes are structured and durable.
Duplicate SKU-in-message ambiguity is handled conservatively.
Parser-side `row_publishable` is treated as advisory, not final custody.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_quote_validation -v</automated>
  </verify>
  <done>The validator can now classify rows conservatively according to real business safety rules instead of parser-only hints.</done>
</task>

<task type="auto">
  <name>Task 2: Expose durable `publishable_rows` helpers and integration regressions</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/tests/test_runtime.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <action>Add query helpers that return the latest validation run for a quote document and the subset of row decisions that are formally publishable. The helper surface must be suitable for later Phase 03 publisher consumption and must never fall back to parser-side `row_publishable`. Add regression coverage that asserts mixed row outcomes are persisted correctly and that only formally publishable rows are returned by the helper.</action>
  <acceptance_criteria>
`database.py` exposes a helper for latest validation run and a helper for `publishable_rows`.
The helper reads validator results, not parser-side candidate flags.
Runtime/replay regressions prove mixed-outcome persistence and correct `publishable_rows` lookup.
No publish or active-fact mutation is introduced in this plan.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_quote_validation tests.test_runtime tests.test_webapp -v</automated>
  </verify>
  <done>Later publisher work has a durable, validator-owned `publishable_rows` surface to consume.</done>
</task>

</tasks>

<threat_model>
- If validator outcomes cannot represent mixed rows, later publisher code will tend to over-simplify messages into all-or-nothing behavior.
- If `publishable_rows` is not durable, later phases may quietly regress to parser-side heuristics.
- If duplicate or ambiguous rows are not handled conservatively, the system risks publishing rows that were not actually safe to trust.
</threat_model>

<verification>
This plan is incomplete unless mixed-outcome validator tests and `publishable_rows` integration tests pass on PostgreSQL.
</verification>

<success_criteria>
The repo has a durable validator decision layer that cleanly separates `publishable_rows`, rejected rows, and held rows for each message.
</success_criteria>

<output>
After completion, create `.planning/phases/02-validation-engine/02-03-SUMMARY.md`
</output>

