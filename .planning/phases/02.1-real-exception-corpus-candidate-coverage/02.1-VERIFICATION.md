---
phase: 02.1-real-exception-corpus-candidate-coverage
verified: 2026-04-14T04:52:34+07:00
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
---

# Phase 02.1 Verification Report

**Phase Goal:** Build a refreshable real-exception corpus and improve candidate coverage for top-volume live quote shapes without weakening validator or fact-protection custody.
**Verified:** 2026-04-14T04:52:34+07:00
**Status:** passed

## Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | The repo now carries a refreshable live-exception corpus plus curated gold fixtures | ✓ VERIFIED | `scripts/refresh_quote_exception_corpus.py`, `tests/fixtures/quote_exception_corpus/index.json`, `gold_top8.json`, and `approved_top8.json`; locked by `tests/test_quote_exception_corpus.py`. |
| 2 | Gold fixtures preserve message-level and row-level business outcomes, including trusted vs pending state | ✓ VERIFIED | `gold_top8.json` stores `message_decision`, `expected_sections`, `expected_rows`, and `approval_status`; `approved_top8.json` records the operator-approved top 8; verified by `tests.test_quote_exception_corpus`. |
| 3 | Deterministic parser hardening now handles scoped numeric rows, shorthand rows, and token noise conservatively | ✓ VERIFIED | `bookkeeping_core/template_engine.py` and `bookkeeping_core/quotes.py` now carry scoped-line analysis and scope evidence; verified by `tests.test_template_engine` and `tests.test_quote_parser`. |
| 4 | Bootstrap tooling consumes only approved fixtures and never writes facts or profiles by itself | ✓ VERIFIED | `scripts/bootstrap_quote_group_profiles.py` only emits JSON payloads from approved fixtures; `tests.test_bootstrap_quote_group_profiles` proves allowlist enforcement and zero DB writes. |
| 5 | Bootstrapped top-volume groups persist candidate rows and validation runs through runtime without mutating active quote facts | ✓ VERIFIED | `tests.test_runtime` now covers `sk_steam_price_update`, `yingzi_steam_razer`, and `yangyang_supermarket_updates`, asserting candidate rows exist and `quote_price_rows` stays unchanged. |
| 6 | Bootstrapped replay remains candidate-only and unapproved fixtures still stay in the exception path | ✓ VERIFIED | `tests.test_webapp` asserts replay returns `mutated_active_facts = false`; `tests.test_runtime` asserts an unapproved fixture still yields `missing_group_template` and zero active-fact writes. |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Corpus, parser hardening, and bootstrap generator regressions stay green together | `cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_quote_exception_corpus tests.test_template_engine tests.test_quote_parser tests.test_bootstrap_quote_group_profiles -v` | `Ran 113 tests`, `OK` | ✓ PASS |
| Runtime stays candidate-only after bootstrap coverage improvements | `cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_runtime -v` | `Ran 43 tests`, `OK` | ✓ PASS |
| Web replay stays candidate-only after bootstrap coverage improvements | `cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_webapp -v` | `Ran 67 tests`, `OK` | ✓ PASS |

## Requirements Coverage

| Requirement | Description | Status | Evidence |
| --- | --- | --- | --- |
| `CORP-01` | Refreshable live exception corpus exists and stays indexable | ✓ SATISFIED | `refresh_quote_exception_corpus.py`, corpus fixtures, `tests.test_quote_exception_corpus` |
| `CORP-02` | Gold fixtures and approval manifest preserve durable trusted-vs-pending state | ✓ SATISFIED | `gold_top8.json`, `approved_top8.json`, `tests.test_quote_exception_corpus` |
| `COV-01` | Deterministic candidate generation improves dominant real exception shapes | ✓ SATISFIED | `template_engine.py`, `quotes.py`, `tests.test_template_engine`, `tests.test_quote_parser` |
| `COV-02` | Bootstrap coverage improves top missing-template groups without mutating active facts | ✓ SATISFIED | `bootstrap_quote_group_profiles.py`, `tests.test_bootstrap_quote_group_profiles`, `tests.test_runtime`, `tests.test_webapp` |

## Residual Notes

- This phase improved candidate reach and bootstrap coverage, but it intentionally did not centralize publish authority. Phase 3 still needs to make guarded publisher custody the only fact-mutation path.
- `graphify` rebuild was attempted separately after code edits but remains environment-dependent because the local `graphify` module is missing.
