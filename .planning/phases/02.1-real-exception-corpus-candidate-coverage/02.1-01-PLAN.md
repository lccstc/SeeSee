---
phase: 02.1-real-exception-corpus-candidate-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wxbot/bookkeeping-platform/scripts/refresh_quote_exception_corpus.py
  - wxbot/bookkeeping-platform/tests/support/quote_exception_corpus.py
  - wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/index.json
  - wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json
  - wxbot/bookkeeping-platform/tests/test_quote_exception_corpus.py
autonomous: false
requirements:
  - CORP-01
  - CORP-02
must_haves:
  truths:
    - "The repo contains a refreshable exception corpus index that records the current top-8-heavy-chat slice and long-tail buckets."
    - "Curated gold samples store message-level and row-level expected outcomes instead of quote-like-line notes only."
    - "High-frequency gold samples require explicit operator approval before they are treated as trusted fixtures."
  artifacts:
    - path: "wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/index.json"
      provides: "Refreshable live-pool index with counts, buckets, and top-chat ranking."
      contains: "\"top_8_exception_count\""
    - path: "wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json"
      provides: "Replayable gold samples with message and row judgments."
      contains: "\"row_decision\""
    - path: "wxbot/bookkeeping-platform/scripts/refresh_quote_exception_corpus.py"
      provides: "Corpus refresh CLI from live API or offline JSON."
      contains: "def main("
    - path: "wxbot/bookkeeping-platform/tests/support/quote_exception_corpus.py"
      provides: "Fixture loader helpers used by later plans."
      contains: "def load_gold_samples("
  key_links:
    - from: "wxbot/bookkeeping-platform/scripts/refresh_quote_exception_corpus.py"
      to: "wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/index.json"
      via: "normalized corpus export"
      pattern: "index.json"
    - from: "wxbot/bookkeeping-platform/tests/support/quote_exception_corpus.py"
      to: "wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json"
      via: "fixture loader"
      pattern: "gold_top8.json"
---

<objective>
Build the refreshable corpus and gold-sample baseline that Phase 02.1 candidate hardening must stand on.

Purpose: convert the live exception pool into durable repo artifacts that can drive replay, fixtures, and regression work instead of staying in prose or one-off API pulls.
Output: a refresh CLI, a corpus index, curated gold fixtures, fixture-loader helpers, and a blocking operator approval checkpoint for the highest-volume samples.
</objective>

<execution_context>
@/Users/newlcc/.codex/get-shit-done/workflows/execute-plan.md
@/Users/newlcc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-CONTEXT.md
@.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-RESEARCH.md
@.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-VALIDATION.md
@.planning/research/REAL-EXCEPTION-CORPUS.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py

<interfaces>
From wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py:
```python
@dataclass(slots=True)
class QuoteCandidateRow:
    source_line: str
    normalized_sku_key: str
    card_type: str | None
    country_or_currency: str | None
    amount_range: str | None
    form_factor: str | None
    price: float | None
    field_sources: dict[str, Any] = field(default_factory=dict)

@dataclass(slots=True)
class QuoteCandidateMessage:
    raw_message: str
    snapshot_hypothesis: str
    snapshot_hypothesis_reason: str
    rejection_reasons: list[dict[str, Any]] = field(default_factory=list)
    rows: list[QuoteCandidateRow] = field(default_factory=list)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create refreshable corpus index artifacts and loader helpers</name>
  <read_first>
.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-CONTEXT.md
.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-RESEARCH.md
.planning/research/REAL-EXCEPTION-CORPUS.md
wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py
  </read_first>
  <files>wxbot/bookkeeping-platform/scripts/refresh_quote_exception_corpus.py, wxbot/bookkeeping-platform/tests/support/quote_exception_corpus.py, wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/index.json</files>
  <behavior>
    - Test 1: Loading `index.json` returns the 2026-04-14 baseline metadata (`226` open exceptions, `25` chats, top-8 total `159`) and exposes top-chat ranking helpers.
    - Test 2: The refresh CLI accepts `--source-url` or `--source-json` plus `--output-dir` and produces the same normalized index shape for later plans.
    - Test 3: Long-tail chats remain visible through bucket summaries even when they are outside the top-8 slice.
  </behavior>
  <action>Per D-01 to D-04 and D-15, create `refresh_quote_exception_corpus.py` as a deterministic CLI that can read either the live exception endpoint payload or an offline JSON export and write a normalized corpus index under `tests/fixtures/quote_exception_corpus/`. Seed `index.json` with the current planning baseline from `REAL-EXCEPTION-CORPUS.md`: `226` open exceptions across `25` chats, top-8 concentration `159`, reason counts `missing_group_template=149` and `strict_match_failed=77`, plus bucket names matching the research note (`multi_section_board`, `quote_plus_rule_text`, `manual_or_ask_lines`, `context_dependent_numeric_rows`, `token_only_source_line`). Add `tests/support/quote_exception_corpus.py` with `load_exception_corpus_index()`, `list_top_exception_chats(limit=8)`, and `list_bucket_members(bucket_name)` helpers. Do not fetch or write anything related to validator or publisher state.</action>
  <acceptance_criteria>
`wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/index.json` contains `"total_open_exceptions": 226`, `"chat_count": 25`, `"top_8_exception_count": 159`, `"missing_group_template": 149`, and `"strict_match_failed": 77`.
`wxbot/bookkeeping-platform/scripts/refresh_quote_exception_corpus.py` accepts both `--source-json` and `--source-url`.
`wxbot/bookkeeping-platform/tests/support/quote_exception_corpus.py` exports `load_exception_corpus_index` and `list_top_exception_chats`.
No file in this task reads or writes `quote_price_rows`, validator tables, or publish logic.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_quote_exception_corpus -v</automated>
  </verify>
  <done>The repository has a refreshable, code-readable corpus index that later plans can consume without scraping prose or live endpoints directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Curate replayable gold fixtures for top exception-heavy chats</name>
  <read_first>
.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-CONTEXT.md
.planning/research/REAL-EXCEPTION-CORPUS.md
wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py
wxbot/bookkeeping-platform/tests/support/quote_exception_corpus.py
  </read_first>
  <files>wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json, wxbot/bookkeeping-platform/tests/test_quote_exception_corpus.py</files>
  <behavior>
    - Test 1: Every curated sample carries `message_decision` and row-level `row_decision` values, not just quote-like lines.
    - Test 2: All current top-8 chats have at least one curated sample fixture.
    - Test 3: Representative samples cover the dominant buckets from research, including mixed boards, manual-rule lines, scoped numeric rows, missing-template boards, shorthand rows, and pure noise.
  </behavior>
  <action>Per D-05 to D-10 and D-14 to D-16, add `gold_top8.json` containing curated samples for the named representative exceptions from the research note: `244`, `242`, `241`, `222`, `169`, `243`, `238`, `236`, `235`, `239`, `198`, `230`, `174`, and `177`. Each fixture must include a stable `fixture_name`, `exception_id`, `platform`, `chat_id`, `chat_name`, `reason`, `raw_text`, `message_decision` (`full_snapshot`, `delta_update`, or `unresolved`), `message_decision_reason`, `approval_status`, `expected_sections`, and `expected_rows`. Each `expected_rows` entry must include normalized `card_type`, `country_or_currency`, `amount_range`, `form_factor`, `price`, and `row_decision` (`publishable`, `held`, or `rejected`). Add `tests.test_quote_exception_corpus` assertions that reject fixtures missing these fields and that confirm the top-8 chats are represented. Keep low-frequency proposals marked as pending until confirmed; do not label them as trusted gold by default.</action>
  <acceptance_criteria>
`wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json` contains fixture records for exception IDs `244`, `242`, `241`, `222`, `169`, `243`, `238`, `236`, `235`, `239`, `198`, `230`, `174`, and `177`.
Every fixture record contains `"message_decision"`, `"approval_status"`, and `"expected_rows"`.
Every `expected_rows` entry contains `"card_type"`, `"country_or_currency"`, `"amount_range"`, `"form_factor"`, `"price"`, and `"row_decision"`.
`tests.test_quote_exception_corpus` fails if any current top-8 chat lacks a curated fixture.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_quote_exception_corpus -v</automated>
  </verify>
  <done>The repo contains replayable gold fixtures for the highest-value live exception shapes and later plans can consume them directly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve high-frequency gold judgments before they become trusted fixtures</name>
  <read_first>
.planning/research/REAL-EXCEPTION-CORPUS.md
wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json
wxbot/bookkeeping-platform/tests/test_quote_exception_corpus.py
  </read_first>
  <files>wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json, wxbot/bookkeeping-platform/tests/test_quote_exception_corpus.py</files>
  <action>Per D-08 to D-10, pause after the corpus artifacts and fixture validations are in place, then have the operator review the curated top-volume samples before they become trusted gold. Keep the checkpoint scoped to semantic correctness of the gold judgments; do not change validator/publisher logic here. If the operator requests corrections, update the exact fixture records and rerun the corpus test module before resuming.</action>
  <what-built>Refreshable corpus assets and curated gold fixtures for the top exception-heavy chats.</what-built>
  <how-to-verify>
1. Open `wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json`.
2. Review the high-frequency fixtures for exception IDs `244`, `242`, `241`, `222`, `169`, `243`, `238`, and `236`.
3. Confirm the `message_decision`, `message_decision_reason`, and each row's `row_decision` match the intended business outcome for those chats.
4. If corrections are needed, list the fixture name plus the exact field changes; otherwise approve the gold set.
  </how-to-verify>
  <acceptance_criteria>
The operator explicitly approves or amends the high-frequency fixtures for IDs `244`, `242`, `241`, `222`, `169`, `243`, `238`, and `236`.
No fixture is treated as trusted gold for top-volume chats without an explicit approval response.
If corrections are requested, they identify the fixture name and the exact field(s) to change.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_quote_exception_corpus -v</automated>
  </verify>
  <done>The high-frequency gold fixtures are explicitly approved or corrected, and the corpus module remains green after any amendments.</done>
  <resume-signal>Reply with `approved` to continue, or list fixture corrections by `fixture_name -> field=value`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live exception export -> committed fixture files | external operational data becomes trusted planning/test material |
| gold fixture authoring -> later parser tests | mislabeled gold data could mis-train later deterministic logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-01 | T | `gold_top8.json` | mitigate | Require explicit `approval_status`, preserve raw message text, and block on operator review for top-volume chats. |
| T-02.1-02 | I | `index.json` | mitigate | Record source endpoint/date/count metadata and keep the refresh CLI deterministic so drift is visible in diffs. |
| T-02.1-03 | R | corpus refresh script | mitigate | Make the CLI write explicit fetch metadata and preserve exception IDs/chat IDs for auditability. |
</threat_model>

<verification>
Run the corpus test module after each task. Do not proceed past the checkpoint until the operator confirms the high-frequency gold fixtures.
</verification>

<success_criteria>
Phase 02.1 has a refreshable corpus index plus approved gold fixtures that later deterministic candidate work can consume directly.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-01-SUMMARY.md`
</output>
