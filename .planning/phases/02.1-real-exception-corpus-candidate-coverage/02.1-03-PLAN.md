---
phase: 02.1-real-exception-corpus-candidate-coverage
plan: 03
type: execute
wave: 3
depends_on:
  - 02.1-01
  - 02.1-02
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py
  - wxbot/bookkeeping-platform/scripts/bootstrap_quote_group_profiles.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - COV-02
must_haves:
  truths:
    - "Top-volume missing-template chats can be bootstrapped into deterministic `group-parser-v1` configs from curated gold samples."
    - "Runtime and replay using those bootstrapped configs persist candidate and validation evidence without mutating active facts."
    - "Unbootstrapped or weak-context chats still stay in the exception path instead of inventing facts."
  artifacts:
    - path: "wxbot/bookkeeping-platform/scripts/bootstrap_quote_group_profiles.py"
      provides: "Deterministic CLI for generating top-chat group-profile bootstrap payloads."
      contains: "--fixture"
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py"
      provides: "Helper that turns gold sample structure into `group-parser-v1` sections."
      contains: "group-parser-v1"
    - path: "wxbot/bookkeeping-platform/tests/test_runtime.py"
      provides: "Runtime regressions for bootstrapped top-volume groups."
      contains: "bootstrap_profile"
    - path: "wxbot/bookkeeping-platform/tests/test_webapp.py"
      provides: "Replay/web regressions proving candidate-only custody with bootstrapped configs."
      contains: "bootstrap_profile"
  key_links:
    - from: "wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json"
      to: "wxbot/bookkeeping-platform/scripts/bootstrap_quote_group_profiles.py"
      via: "fixture-to-template generation"
      pattern: "gold_top8.json"
    - from: "wxbot/bookkeeping-platform/scripts/bootstrap_quote_group_profiles.py"
      to: "wxbot/bookkeeping-platform/tests/test_runtime.py"
      via: "generated profile config seeded into group profile"
      pattern: "group-parser-v1"
---

<objective>
Turn the curated top-volume samples into deterministic group bootstrap tooling and prove the resulting coverage through runtime/replay regressions.

Purpose: improve the highest-value missing-template groups inside the existing one-group-one-template workflow, without introducing runtime auto-publish shortcuts or validator bypasses.
Output: a bootstrap generator/helper for top chats plus runtime/web regressions showing the generated configs stay candidate-only and replay-safe.
</objective>

<execution_context>
@/Users/newlcc/.codex/get-shit-done/workflows/execute-plan.md
@/Users/newlcc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-CONTEXT.md
@.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-RESEARCH.md
@AGENTS.md
@wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json
@wxbot/bookkeeping-platform/tests/support/quote_exception_corpus.py
@wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py
@wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
@wxbot/bookkeeping-platform/tests/test_runtime.py
@wxbot/bookkeeping-platform/tests/test_webapp.py

<interfaces>
From wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py:
```python
@dataclass
class TemplateConfig:
    version: str
    defaults: dict[str, Any]
    rules: list[dict[str, str]] = field(default_factory=list)
    sections: list[dict[str, Any]] = field(default_factory=list)

def parse_message_with_template(
    text: str,
    template: TemplateConfig,
    *,
    platform: str = "",
    chat_id: str = "",
    ...
) -> ParsedQuoteDocument: ...
```

From wxbot/bookkeeping-platform/bookkeeping_core/quotes.py:
```python
def parse_quote_message_to_candidate(
    *,
    envelope: NormalizedMessageEnvelope,
    raw_text: str,
    group_profile: QuoteGroupProfile,
    message_time: str,
    run_kind: str = "runtime",
) -> QuoteCandidateMessage: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate deterministic bootstrap profiles for top missing-template chats</name>
  <read_first>
.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-CONTEXT.md
wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json
wxbot/bookkeeping-platform/tests/support/quote_exception_corpus.py
wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py
  </read_first>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py, wxbot/bookkeeping-platform/scripts/bootstrap_quote_group_profiles.py</files>
  <behavior>
    - Test 1: Given a named gold fixture, the generator emits a valid `group-parser-v1` template payload with sections/defaults derived from approved rows.
    - Test 2: The CLI supports exact fixture names for the highest-volume missing-template chats and prints JSON that can be fed into `upsert_quote_group_profile`.
    - Test 3: The generator never writes to the database or touches publisher paths by itself.
  </behavior>
  <action>Per D-01, D-02, D-11, D-14, and D-20 to D-22, add a deterministic helper in `template_engine.py` plus a CLI in `scripts/bootstrap_quote_group_profiles.py` that turns approved gold fixtures into `group-parser-v1` profile payloads. Support at least these fixture names from Plan 01: `sk_steam_price_update`, `yingzi_steam_razer`, `yangyang_supermarket_updates`, `wanrui_xbox_rules`, `qh_delta_numeric_update`, `xixi_multi_section_itunes_xbox_razer`, `yangyang_nordstrom_pic_code`, and `wannuo_xb_shorthand`. The script must accept `--fixture`, `--platform`, `--chat-id`, and `--output`/stdout so operators can generate a bootstrap config inside the existing one-group-one-template flow. Do not auto-apply runtime fallbacks for missing templates and do not read or write validator/publisher state.</action>
  <acceptance_criteria>
`wxbot/bookkeeping-platform/scripts/bootstrap_quote_group_profiles.py` documents or parses `--fixture`, `--platform`, and `--chat-id`.
Generator output contains `"version": "group-parser-v1"` and `sections`.
Supported fixture names include `sk_steam_price_update`, `yingzi_steam_razer`, and `yangyang_supermarket_updates`.
No file in this task calls DB persistence helpers or mutates `quote_price_rows`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_runtime tests.test_webapp -v</automated>
  </verify>
  <done>The repo can generate deterministic top-chat bootstrap configs without creating a bypass around validator/publisher custody.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove bootstrap coverage through runtime and replay regressions</name>
  <read_first>
wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json
wxbot/bookkeeping-platform/scripts/bootstrap_quote_group_profiles.py
wxbot/bookkeeping-platform/bookkeeping_web/app.py
wxbot/bookkeeping-platform/tests/test_runtime.py
wxbot/bookkeeping-platform/tests/test_webapp.py
  </read_first>
  <files>wxbot/bookkeeping-platform/tests/test_runtime.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <behavior>
    - Test 1: Runtime capture with a generated bootstrap profile persists candidate rows and a validation run while leaving `quote_price_rows` untouched.
    - Test 2: Replay/web flows using the generated profile report candidate-only success (`mutated_active_facts` false) for top-volume fixtures.
    - Test 3: An unbootstrapped or unapproved sample still follows the exception path instead of inventing candidates.
  </behavior>
  <action>Per D-01, D-02, D-14, D-17, and D-20 to D-22, add integration regressions in `tests.test_runtime` and `tests.test_webapp` that seed generated profile configs for at least three top-volume missing-template fixtures: `sk_steam_price_update`, `yingzi_steam_razer`, and `yangyang_supermarket_updates`. Exercise runtime quote capture and replay/web flows so they prove: candidate bundles and validation runs persist, `quote_price_rows` stays unchanged, and replay remains candidate-only. Add one negative regression for an unbootstrapped or still-pending fixture so the system continues to emit `missing_group_template` or `strict_match_failed` rather than fabricating a row. Reuse the existing runtime/replay helpers; do not introduce publisher behavior.</action>
  <acceptance_criteria>
`wxbot/bookkeeping-platform/tests/test_runtime.py` includes bootstrap-profile coverage for `sk_steam_price_update`, `yingzi_steam_razer`, and `yangyang_supermarket_updates`.
`wxbot/bookkeeping-platform/tests/test_webapp.py` asserts replay remains candidate-only with `mutated_active_facts` false for a bootstrapped fixture.
At least one regression proves an unbootstrapped or unapproved fixture still lands in the exception path.
The integration tests assert `quote_price_rows` remains unchanged.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_runtime tests.test_webapp -v</automated>
  </verify>
  <done>The highest-value missing-template chats have a deterministic bootstrap path that improves coverage without compromising candidate-only custody.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| approved gold fixture -> generated group profile | curated test material becomes operator tooling that shapes runtime parsing |
| generated group profile -> runtime/replay capture | bootstrap templates influence candidate generation for real message flows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-07 | T | bootstrap generator | mitigate | Generate templates only from approved fixtures and keep fixture names explicit in CLI inputs/output. |
| T-02.1-08 | E | runtime/replay path | mitigate | Add regressions asserting `quote_price_rows` remains unchanged and replay reports candidate-only success with `mutated_active_facts` false. |
| T-02.1-09 | S | missing-template fallback temptation | mitigate | Do not auto-wire unknown chats into bootstraps; keep one negative regression that preserves exception behavior for unapproved fixtures. |
</threat_model>

<verification>
Run the runtime and web suites after each task. Any solution that improves parsing by bypassing group profiles or mutating active facts fails this plan.
</verification>

<success_criteria>
Top-volume missing-template coverage improves through deterministic bootstrap tooling and integration proofs, while validator ownership and active-fact safety remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-03-SUMMARY.md`
</output>
