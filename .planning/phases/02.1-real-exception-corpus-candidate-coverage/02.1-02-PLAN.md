---
phase: 02.1-real-exception-corpus-candidate-coverage
plan: 02
type: execute
wave: 2
depends_on:
  - 02.1-01
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py
  - wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
  - wxbot/bookkeeping-platform/tests/test_template_engine.py
  - wxbot/bookkeeping-platform/tests/test_quote_parser.py
autonomous: true
requirements:
  - COV-01
must_haves:
  truths:
    - "Real multi-section boards produce candidate rows from the right section scope instead of collapsing into one unmatched exception blob."
    - "Rule, manual, and noise lines do not become candidate rows merely because they contain numbers."
    - "Scoped numeric rows carry explicit scope evidence when card/country context is inherited from a nearby header."
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py"
      provides: "Deterministic section splitting, line classification, and scope carry-forward."
      contains: "parse_message_with_template"
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/quotes.py"
      provides: "Candidate evidence mapping for inherited scope."
      contains: "scope_evidence"
    - path: "wxbot/bookkeeping-platform/tests/test_template_engine.py"
      provides: "Corpus-backed parser regressions for dominant failure shapes."
      contains: "scoped_numeric"
    - path: "wxbot/bookkeeping-platform/tests/test_quote_parser.py"
      provides: "Candidate evidence regressions for inherited scope and suppression."
      contains: "scope_evidence"
  key_links:
    - from: "wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json"
      to: "wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py"
      via: "fixture-backed parser tests"
      pattern: "gold_top8.json"
    - from: "wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py"
      to: "wxbot/bookkeeping-platform/bookkeeping_core/quotes.py"
      via: "ParsedQuoteRow -> QuoteCandidateRow evidence mapping"
      pattern: "_parsed_quote_row_to_candidate_row"
---

<objective>
Harden deterministic candidate generation for the dominant real exception shapes without loosening validator or publisher custody.

Purpose: move the parser from "whole board falls into exception" toward conservative but explainable candidate extraction for the shapes dominating the live pool.
Output: fixture-backed RED/GREEN tests, explicit line classification, section splitting, scoped numeric carry-forward, and persisted scope evidence on candidate rows.
</objective>

<execution_context>
@/Users/newlcc/.codex/get-shit-done/workflows/execute-plan.md
@/Users/newlcc/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-CONTEXT.md
@.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-RESEARCH.md
@.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json
@wxbot/bookkeeping-platform/tests/support/quote_exception_corpus.py
@wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py
@wxbot/bookkeeping-platform/bookkeeping_core/quotes.py

<interfaces>
From wxbot/bookkeeping-platform/bookkeeping_core/quotes.py:
```python
@dataclass(slots=True)
class ParsedQuoteRow:
    card_type: str
    country_or_currency: str
    amount_range: str
    form_factor: str
    price: float
    source_line: str
    raw_text: str
    parser_template: str
    confidence: float

def _parsed_quote_row_to_candidate_row(
    row: ParsedQuoteRow,
    *,
    row_ordinal: int,
) -> QuoteCandidateRow: ...
```

From wxbot/bookkeeping-platform/bookkeeping_core/quote_candidates.py:
```python
class QuoteCandidateRow:
    source_line_index: int | None
    field_sources: dict[str, Any]
    card_type: str | None
    country_or_currency: str | None
    amount_range: str | None
    form_factor: str | None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add corpus-backed RED tests for dominant structural failures</name>
  <read_first>
.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-CONTEXT.md
wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json
wxbot/bookkeeping-platform/tests/support/quote_exception_corpus.py
wxbot/bookkeeping-platform/tests/test_template_engine.py
wxbot/bookkeeping-platform/tests/test_quote_parser.py
wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py
wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
  </read_first>
  <files>wxbot/bookkeeping-platform/tests/test_template_engine.py, wxbot/bookkeeping-platform/tests/test_quote_parser.py</files>
  <behavior>
    - Test 1: Fixture `feihong_it_scoped_numeric` keeps `50=5.25` and `100/150=5.41` tied to the preceding header scope instead of dropping them or inventing unrelated scope.
    - Test 2: Fixture `panda_supermarket_manual_lines` treats `问价`, `请勿直发`, and rule text as non-candidate content even when nearby quote rows are valid.
    - Test 3: Fixtures `mile_it_shift_update`, `qh_delta_numeric_update`, `wannuo_xb_shorthand`, and `qing_song_noise_token` cover shift-update scoped rows, shorthand rows, and pure noise suppression.
  </behavior>
  <action>Per D-03, D-11, D-13, and D-17 to D-19, extend `tests.test_template_engine` and repurpose `tests.test_quote_parser` from the current placeholder into corpus-backed regression modules. Add exact fixture-driven tests for `feihong_it_scoped_numeric`, `panda_supermarket_manual_lines`, `mile_it_shift_update`, `qh_delta_numeric_update`, `wannuo_xb_shorthand`, and `qing_song_noise_token`. `tests.test_template_engine` should assert row extraction/classification outcomes; `tests.test_quote_parser` should assert the candidate evidence produced after `ParsedQuoteRow -> QuoteCandidateRow`, including `field_sources.line_evidence` and the new inherited-scope evidence payload. Keep the tests red until Task 2 implements the parser changes.</action>
  <acceptance_criteria>
`wxbot/bookkeeping-platform/tests/test_template_engine.py` contains fixture-backed tests for `feihong_it_scoped_numeric`, `panda_supermarket_manual_lines`, `mile_it_shift_update`, `qh_delta_numeric_update`, `wannuo_xb_shorthand`, and `qing_song_noise_token`.
`wxbot/bookkeeping-platform/tests/test_quote_parser.py` is no longer a doc-only placeholder and asserts candidate evidence for inherited scope.
The tests distinguish quote rows from `rule`, `manual`, and `noise` content instead of checking only row counts.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_template_engine tests.test_quote_parser -v</automated>
  </verify>
  <done>The dominant live failure shapes are locked into failing tests before parser code changes begin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic section splitting, line classification, and scope evidence</name>
  <read_first>
.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-RESEARCH.md
wxbot/bookkeeping-platform/tests/fixtures/quote_exception_corpus/gold_top8.json
wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py
wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
wxbot/bookkeeping-platform/tests/test_template_engine.py
wxbot/bookkeeping-platform/tests/test_quote_parser.py
  </read_first>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py, wxbot/bookkeeping-platform/bookkeeping_core/quotes.py, wxbot/bookkeeping-platform/tests/test_template_engine.py, wxbot/bookkeeping-platform/tests/test_quote_parser.py</files>
  <behavior>
    - Test 1: Multi-section boards preserve the nearest valid section/header context for numeric quote rows and surface unmatched leftovers as exceptions.
    - Test 2: `ask`, manual, rule, and pure noise lines are classified out early and do not leak into candidate rows.
    - Test 3: When scope is inherited, candidate `field_sources` includes explicit `scope_evidence` showing the header line, header line index, and inherited fields.
  </behavior>
  <action>Per D-11 to D-13 and D-17 to D-22, modify `template_engine.py` so the group-parser path explicitly classifies normalized lines as `header`, `quote`, `rule`, `inquiry`, or `noise`, then performs conservative scope carry-forward only when a nearby header or section default can justify the inheritance. Support the dominant shorthand forms called out in the corpus (`50=5.25`, `100/150=5.41`, `+100 xb 5.13`) only when card/country/form-factor context is explicit; otherwise leave the line in exceptions. In `quotes.py`, extend `ParsedQuoteRow` and `_quote_row_field_sources(...)` so inherited card/country/form-factor values produce a `scope_evidence` payload with the header text, header line index, and the list of inherited fields. Do not relax validator thresholds, do not mark held rows as publishable, and do not touch publisher paths.</action>
  <acceptance_criteria>
`wxbot/bookkeeping-platform/bookkeeping_core/quotes.py` contains the string `scope_evidence`.
Fixture-backed tests for `feihong_it_scoped_numeric`, `mile_it_shift_update`, `qh_delta_numeric_update`, `wannuo_xb_shorthand`, and `qing_song_noise_token` pass.
Rule/manual fixtures do not create candidate rows solely because they contain numbers.
No file in this task modifies validator reason codes, `publishable_rows` logic, or active-quote mutation paths.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_template_engine tests.test_quote_parser -v</automated>
  </verify>
  <done>Deterministic candidate generation handles the dominant real structural failures conservatively and leaves explicit evidence for downstream validation/replay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw vendor message -> parser classification | untrusted mixed-content message becomes candidate or exception evidence |
| parser row -> candidate field sources | inferred scope must remain explainable rather than silently fabricated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-04 | T | `template_engine.py` classification path | mitigate | Add corpus-backed tests that keep rule/manual/noise lines out of candidate rows even when numbers are present. |
| T-02.1-05 | I | inherited scope evidence | mitigate | Persist explicit `scope_evidence` with header text/index and inherited-field list instead of hiding carry-forward inside normalized fields. |
| T-02.1-06 | E | candidate rows bypassing validator posture | mitigate | Keep changes candidate-only; do not modify validator/publisher code or final publish decisions in this plan. |
</threat_model>

<verification>
The quick parser suite is mandatory after each task. Any fix that increases row counts but drops evidence or promotes rule text is a failure.
</verification>

<success_criteria>
The dominant live exception shapes now produce more usable candidate bundles, but only when the parser can justify them with explicit deterministic evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-02-SUMMARY.md`
</output>
