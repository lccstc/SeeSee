---
phase: 06-constrained-auto-remediation-loop
plan: 03
type: execute
wave: 3
depends_on:
  - 06-02-PLAN.md
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/remediation.py
  - wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/tests/test_template_engine.py
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - INDU-01
  - INDU-02
must_haves:
  truths:
    - "A remediation proposal is absorbed only after replay, validator, and regression gates all pass."
    - "Successful repairs leave deterministic artifacts behind instead of one-off prompt wins."
    - "Failed repairs close the attempt with explicit evidence and escalate after repeated failure instead of looping forever."
  key_links:
    - from: "bookkeeping_core/remediation.py"
      to: "bookkeeping_core/repair_cases.py"
      via: "final attempt outcomes and absorption decisions roll into the existing repair-case history and summaries"
    - from: "bookkeeping_core/remediation.py"
      to: "bookkeeping_web/app.py"
      via: "operator-facing proof text for absorbed/escalated remediation outcomes stays proof-only and non-publishing"
    - from: "bookkeeping_core/remediation.py"
      to: "tests.test_runtime / tests.test_webapp"
      via: "replay+validator+regression gates and exposed wording are proven together"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/remediation.py"
      provides: "Absorption gate orchestration and deterministic industrialization outputs."
      contains: "def finalize_quote_repair_attempt("
    - path: "wxbot/bookkeeping-platform/tests/test_runtime.py"
      provides: "Replay/validator absorption regressions for remediation attempts."
      contains: "remediation"
    - path: "wxbot/bookkeeping-platform/tests/test_webapp.py"
      provides: "Operator-facing wording and repair-summary regressions where needed."
      contains: "repair"
---

<objective>
Close the remediation loop so successful proposals become deterministic system behavior and failed proposals become explicit escalation-ready evidence.

Output: absorption gates, deterministic artifact promotion, and final-attempt escalation semantics built on top of Phase 05 repair cases and Phase 06 scope routing.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-constrained-auto-remediation-loop/06-RESEARCH.md
@.planning/phases/06-constrained-auto-remediation-loop/06-VALIDATION.md
@.planning/phases/05-exception-repair-state-machine/05-VERIFICATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/remediation.py
@wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add replay + validator + regression absorption gates</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/remediation.py, wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py, wxbot/bookkeeping-platform/tests/test_runtime.py</files>
  <action>Implement the final remediation gate that evaluates a proposal after it has been applied in its bounded write scope. The gate must run replay, validator, and scope-matched regression checks, then record absorbed / rejected / escalated outcomes on the remediation attempt summary. Preserve the rule that replay remains candidate-only and cannot mutate active quote facts. Rejected attempts must append durable failure evidence; absorbed attempts must mark which gates passed.</action>
  <acceptance_criteria>
Absorption requires replay + validator + regression success.
Rejected attempts persist gate results and failure evidence.
Replay remains candidate-only with no `quote_price_rows` mutation.
Tests prove the system distinguishes absorbed, rejected, and escalated outcomes.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime -v</automated>
  </verify>
  <done>The workflow now has a real system gate between “proposal exists” and “repair absorbed”.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Industrialize successful repairs into deterministic artifacts and escalation-ready outputs</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/remediation.py, wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/tests/test_template_engine.py, wxbot/bookkeeping-platform/tests/test_runtime.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <action>When a remediation attempt passes all gates, emit deterministic absorption artifacts appropriate to its scope: group profile/section changes, bootstrap updates, shared-rule updates, and targeted fixtures/tests. When it fails, produce structured escalation-ready summaries that explain the failure, the chosen scope, the previous attempts consumed, and why the case is now escalated. Where operator-facing wording exists, ensure the language remains proof-only and never implies active-quote mutation or publish success.</action>
  <acceptance_criteria>
Successful repairs leave behind deterministic artifacts, not just attempt logs.
Repeated failures produce structured escalation summaries suitable for main-agent or human follow-up.
Any exposed wording still distinguishes absorbed grammar repairs from quote publication.
Regression coverage proves successful repairs are durable and failed repairs are explicit.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_template_engine tests.test_postgres_backend tests.test_runtime tests.test_webapp -v</automated>
  </verify>
  <done>Repairs now either become deterministic system knowledge or escalate with explicit evidence; nothing remains a hidden one-off agent success.</done>
</task>

</tasks>

<threat_model>
- If replay/validator/regression gates are optional, remediation will smuggle unproven changes into the grammar layer.
- If successful repairs do not emit deterministic artifacts, the system will keep depending on prompt memory.
- If escalation summaries are weak or ambiguous, Phase 06 will hide repeated failure instead of surfacing it.
</threat_model>

<verification>
Wave 3 is complete only when absorbed repairs are deterministic, rejected repairs are explicit, and exposed wording still communicates proof-only behavior.
</verification>

<success_criteria>
Phase 06 finishes with a constrained remediation workflow that can safely absorb proven grammar fixes, reject unsafe proposals, and escalate stubborn cases instead of looping forever.
</success_criteria>

<output>
After completion, create `.planning/phases/06-constrained-auto-remediation-loop/06-03-SUMMARY.md`
</output>
