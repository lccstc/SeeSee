---
phase: 06-constrained-auto-remediation-loop
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01-PLAN.md
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/remediation.py
  - wxbot/bookkeeping-platform/tests/test_template_engine.py
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
autonomous: true
requirements:
  - INDU-02
must_haves:
  truths:
    - "The system enforces the exact remediation order `group_profile -> group_section -> bootstrap -> shared_rule -> global_core`."
    - "Subagent repair proposals are constrained to safe write scopes tied to candidate generation, templates, bootstrap, and tests."
    - "Promotion from group-level to shared/global logic requires repeated evidence, not one lucky sample."
  key_links:
    - from: "bookkeeping_core/remediation.py"
      to: "quote_repair_cases / quote_repair_case_attempts"
      via: "scope decisions and approved write envelopes are persisted on remediation attempts"
    - from: "bookkeeping_core/remediation.py"
      to: "tests.test_template_engine"
      via: "scope routing regressions prove the ordered decision tree and bootstrap intermediate step"
    - from: "bookkeeping_core/remediation.py"
      to: "tests.test_runtime"
      via: "safe write-boundary enforcement remains fact-neutral and does not cross into publisher custody"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/remediation.py"
      provides: "Scope router and safe write-boundary enforcement for remediation proposals."
      contains: "def choose_quote_repair_scope("
    - path: "wxbot/bookkeeping-platform/tests/test_template_engine.py"
      provides: "Regression coverage for group-first promotion and deterministic scope choices."
      contains: "scope"
---

<objective>
Implement the group-first remediation router and safe write boundaries so repair proposals land in the smallest safe scope first.

Output: deterministic scope selection, explicit promotion rules, and enforced write envelopes for group profile, group section, bootstrap, shared rule, and global core repairs.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-constrained-auto-remediation-loop/06-RESEARCH.md
@.planning/phases/06-constrained-auto-remediation-loop/06-VALIDATION.md
@.planning/phases/02.1-real-exception-corpus-candidate-coverage/02.1-CONTEXT.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/remediation.py
@wxbot/bookkeeping-platform/bookkeeping_core/template_engine.py
@wxbot/bookkeeping-platform/scripts/bootstrap_quote_group_profiles.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode group-first remediation priority and promotion criteria</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/remediation.py, wxbot/bookkeeping-platform/tests/test_template_engine.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <action>Add deterministic routing that classifies a repair proposal into one of five scopes: `group_profile`, `group_section`, `bootstrap`, `shared_rule`, or `global_core`. Encode the exact ordered decision tree from the roadmap: try `group_profile` first; fall through to `group_section` only when the failure is localized to one section and a whole-profile repair would be broader than necessary; fall through to `bootstrap` only when no existing group profile/section can safely absorb the fix but a group-bound bootstrap artifact can; promote to `shared_rule` or `global_core` only after repeated cross-group evidence such as multi-group corpus matches or repeated approved outcomes across multiple chats. Keep this routing system-owned, not LLM-owned.</action>
  <acceptance_criteria>
Single-group failures always attempt `group_profile` before any narrower or broader promotion.
`group_section` is chosen only when explicit section-local conditions are met.
`bootstrap` is treated as an intermediate group-bound scope after `group_section`, not as a shortcut to shared/global repair.
`shared_rule` and `global_core` require repeated cross-group evidence stronger than a single case.
Tests prove the router follows the exact order `group_profile -> group_section -> bootstrap -> shared_rule -> global_core`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_template_engine tests.test_postgres_backend -v</automated>
  </verify>
  <done>The system can explain why a repair belongs at the group, bootstrap, shared-rule, or global-core layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce safe write scopes for subagent remediation proposals</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/remediation.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py, wxbot/bookkeeping-platform/tests/test_runtime.py</files>
  <action>Define admissible write sets per remediation scope. `group_profile` proposals may touch only the owning group's profile defaults/template-config envelope; `group_section` proposals may touch only the owning group's targeted section subtree; `bootstrap` proposals may touch only group-bound bootstrap artifacts; `shared_rule` proposals may touch only explicit deterministic parser helper surfaces after the repeated-evidence gate; `global_core` proposals require the strictest allowlist and broadest regression coverage. Block any proposal that touches publisher custody, snapshot execution, active quote facts, or unrelated group templates. Persist the chosen scope and the validated write envelope on the remediation attempt summary.</action>
  <acceptance_criteria>
Subagent proposals are rejected when they target disallowed files or publisher/fact surfaces.
Allowed write scopes are explicit and machine-readable.
Runtime/web regressions confirm remediation admission never mutates active quote facts.
Attempt summaries record the approved write scope used for the proposal.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime tests.test_webapp -v</automated>
  </verify>
  <done>Repair proposals are now constrained to explicit, safe write envelopes that respect one-group-one-profile boundaries.</done>
</task>

</tasks>

<threat_model>
- If group-level failures promote straight to shared/global rules, the system will recreate cross-group regressions.
- If write scopes are implicit, subagents can accidentally modify publisher or unrelated group surfaces.
- If bootstrap is treated as global, top-group repairs will spread before they are proven.
</threat_model>

<verification>
Wave 2 is complete only when scope routing and write-boundary enforcement both pass regression coverage and remain fact-neutral.
</verification>

<success_criteria>
The remediation workflow now routes repairs to the smallest safe scope first and blocks unsafe write surfaces before any replay/proof work runs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-constrained-auto-remediation-loop/06-02-SUMMARY.md`
</output>
