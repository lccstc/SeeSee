---
phase: 06-constrained-auto-remediation-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/database.py
  - wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py
  - wxbot/bookkeeping-platform/bookkeeping_core/remediation.py
  - wxbot/bookkeeping-platform/sql/postgres_schema.sql
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
autonomous: true
requirements:
  - INDU-01
must_haves:
  truths:
    - "Repair cases can only enter a bounded remediation loop with append-only attempts and explicit escalation after repeated failure."
    - "Every retry must prove it read prior failure history before the system accepts a new remediation attempt."
    - "Remediation attempts remain candidate-side proof events and never mutate active quote facts."
  key_links:
    - from: "bookkeeping_core/remediation.py"
      to: "bookkeeping_core/repair_cases.py"
      via: "bounded remediation attempts extend the existing repair-case attempt substrate instead of creating a second workflow"
    - from: "bookkeeping_core/remediation.py"
      to: "bookkeeping_core/database.py"
      via: "attempt metadata, history-consumption proof, and escalation rollups persist on the existing case/attempt tables"
    - from: "bookkeeping_core/remediation.py"
      to: "tests.test_runtime"
      via: "bounded remediation remains candidate-only and fact-neutral under runtime regressions"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/remediation.py"
      provides: "Constrained remediation-attempt contract and system-owned retry/escalation orchestration."
      contains: "def begin_quote_repair_remediation_attempt("
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py"
      provides: "Repair-case lifecycle integration for remediation attempts."
      contains: "def record_quote_repair_attempt("
    - path: "wxbot/bookkeeping-platform/tests/test_postgres_backend.py"
      provides: "Persistence and escalation regressions for bounded remediation attempts."
      contains: "remediation"
    - path: "wxbot/bookkeeping-platform/tests/test_runtime.py"
      provides: "Runtime-safe regressions proving remediation attempts do not touch active facts."
      contains: "repair"
---

<objective>
Add the bounded remediation-attempt contract on top of Phase 05 repair cases.

Output: system-owned remediation attempt orchestration with max-attempt enforcement, prior-history gating, append-only failure logs, and explicit escalation state.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-constrained-auto-remediation-loop/06-RESEARCH.md
@.planning/phases/06-constrained-auto-remediation-loop/06-VALIDATION.md
@.planning/phases/05-exception-repair-state-machine/05-VERIFICATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/database.py
@wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded remediation-attempt contract and max-attempt escalation</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/remediation.py, wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/sql/postgres_schema.sql, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <action>Create a remediation orchestration layer that opens bounded non-baseline attempts against existing repair cases. Extend the durable attempt substrate only as needed to record remediation metadata such as proposal scope, proposal kind, verification gates, and absorption decision. Enforce max 3 remediation attempts per repair case; a fourth automatic attempt must be rejected and the case must transition to escalation-ready state. Keep attempt history append-only and reuse `quote_repair_case_attempts` / repair-case rollups rather than inventing parallel remediation logs.</action>
  <acceptance_criteria>
Repair cases can enter remediation attempts without adding a second workflow table.
The third failed remediation attempt transitions the case into escalation-ready state.
A fourth automatic remediation attempt is rejected by system logic.
PostgreSQL tests prove attempts remain append-only and bounded.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime -v</automated>
  </verify>
  <done>The repo has a first-class bounded remediation loop instead of informal repeated retries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require prior-failure-history consumption before each retry</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/remediation.py, wxbot/bookkeeping-platform/bookkeeping_core/repair_cases.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py, wxbot/bookkeeping-platform/tests/test_runtime.py</files>
  <action>Add a hard gate so remediation attempt 2 and 3 cannot begin unless the system can prove prior failure history was loaded into the proposal context. Persist a lightweight structured marker such as history fingerprint, prior-attempt count, and consumed failure-log summary on the new attempt metadata. Reject retries that do not declare prior-history consumption. Do not add UI controls here; keep the protocol headless and system-owned.</action>
  <acceptance_criteria>
Retry attempts require explicit history-consumption metadata.
Attempt 2 and 3 fail fast when prior history is missing.
Attempt summaries preserve which failure history was read before retry.
Runtime regressions show retry gating stays fact-neutral and candidate-only.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime -v</automated>
  </verify>
  <done>Retry attempts now prove they learned from earlier failures instead of re-running the same blind patch loop.</done>
</task>

</tasks>

<threat_model>
- If retries are not bounded, remediation becomes an unending prompt loop.
- If retry attempts do not consume prior failure history, the workflow will repeat the same failed patch with different text.
- If remediation attempts can mutate facts, Phase 06 would silently bypass Phase 03 publisher custody.
</threat_model>

<verification>
Wave 1 is complete only when bounded retry logic, max-attempt escalation, and history-consumption gating are all covered by automated tests.
</verification>

<success_criteria>
The remediation workflow now has a formal, bounded attempt protocol with explicit escalation and proof that each retry read earlier failures first.
</success_criteria>

<output>
After completion, create `.planning/phases/06-constrained-auto-remediation-loop/06-01-SUMMARY.md`
</output>
