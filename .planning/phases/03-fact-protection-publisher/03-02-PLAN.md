---
phase: 03-fact-protection-publisher
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py
  - wxbot/bookkeeping-platform/bookkeeping_core/database.py
  - wxbot/bookkeeping-platform/sql/postgres_schema.sql
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
autonomous: true
requirements:
  - FACT-01
  - FACT-02
must_haves:
  truths:
    - "Zero publishable rows produce an explicit no-op before any active-fact mutation begins."
    - "Publish failures roll back cleanly and leave prior active rows untouched."
    - "Publisher execution is serialized per group and guarded by database-backed integrity protections."
  key_links:
    - from: "bookkeeping_core/quote_publisher.py"
      to: "bookkeeping_core/database.py"
      via: "publisher transaction + lock boundary owns all destructive mutation steps"
    - from: "bookkeeping_core/quote_publisher.py"
      to: "sql/postgres_schema.sql"
      via: "publisher assumptions about live-row uniqueness are backed by schema-level constraints where appropriate"
    - from: "tests.test_postgres_backend"
      to: "tests.test_runtime"
      via: "atomic no-op and rollback behavior must hold both directly and through runtime entrypoints"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py"
      provides: "Atomic no-op/apply/rollback guarded publisher flow."
      contains: "publish"
    - path: "wxbot/bookkeeping-platform/sql/postgres_schema.sql"
      provides: "Schema-backed live-row integrity reinforcement for publisher custody."
      contains: "quote_price_rows"
---

<objective>
Add the atomic and default-safe protections that make the guarded publisher trustworthy.

Output: explicit no-op behavior for zero publishable rows, one transaction/lock boundary per publish attempt, and rollback-safe active-row mutation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fact-protection-publisher/03-CONTEXT.md
@.planning/phases/03-fact-protection-publisher/03-RESEARCH.md
@.planning/phases/03-fact-protection-publisher/03-VALIDATION.md
@wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py
@wxbot/bookkeeping-platform/bookkeeping_core/database.py
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make zero-publish and unresolved publish attempts explicit guarded no-ops</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py, wxbot/bookkeeping-platform/tests/test_runtime.py</files>
  <action>Teach the publisher to decide no-op versus apply before any destructive step. A publish request with zero `publishable_rows`, unresolved publish mode, or other disqualifying preconditions must return a structured no-op result and leave `quote_price_rows` untouched. Add regressions that prove the wall remains unchanged for empty publishable sets and publisher-rejected attempts.</action>
  <acceptance_criteria>
Zero `publishable_rows` never changes active quote facts.
Publisher-rejected attempts return explicit no-op results.
Runtime regressions prove no-op behavior without relying on absence of writes.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime -v</automated>
  </verify>
  <done>Publish attempts now fail safe instead of mutating facts by accident.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transaction/locking and rollback-safe publish invariants</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py, wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/sql/postgres_schema.sql, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <action>Wrap guarded publish execution in one PostgreSQL transaction and per-group lock boundary. Add tests that inject a failure after the first destructive step and prove prior active rows remain intact after rollback. Where appropriate, reinforce publisher assumptions with schema-level integrity such as a partial unique index for live rows. Keep snapshot semantics explicit but deferred; this task is about custody and rollback, not about deciding unseen-SKU treatment.</action>
  <acceptance_criteria>
Injected publish failure rolls back all active-row changes.
Per-group publish attempts are serialized by database-backed locking.
Schema/integration regressions prove the live-row invariant still holds under publisher custody.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime -v</automated>
  </verify>
  <done>The guarded publisher now enforces failure-safe atomicity instead of optimistic best effort.</done>
</task>

</tasks>

<threat_model>
- If no-op decisions happen after deactivate/supersede steps, Phase 03 will still violate FACT-02.
- If rollback cannot restore prior active rows after a mid-publish failure, FACT-01 remains false even with a publisher class in repo.
- If lock/integrity strategy stays implicit, concurrency regressions may reintroduce duplicate or missing live rows later.
</threat_model>

<verification>
Wave 2 is complete only when empty/invalid publish requests are explicit no-ops and injected publish failures leave the active wall unchanged.
</verification>

<success_criteria>
The publisher now behaves as a trustworthy guarded gate: explicit no-op on disqualified publishes, atomic apply on qualified publishes, and clean rollback on failure.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fact-protection-publisher/03-02-SUMMARY.md`
</output>
