---
phase: 03-fact-protection-publisher
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py
  - wxbot/bookkeeping-platform/bookkeeping_core/database.py
  - wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
autonomous: true
requirements:
  - FACT-01
  - FACT-03
must_haves:
  truths:
    - "One guarded publisher becomes the only intended core API for active-quote mutation."
    - "Low-level quote mutation helpers participate in publisher-owned transactions rather than committing independently."
    - "Runtime publish entrypoints consume validator-owned publishable rows instead of parser-side publish guesses."
  key_links:
    - from: "bookkeeping_core/quote_publisher.py"
      to: "bookkeeping_core/database.py"
      via: "guarded publisher owns transaction custody while database helpers become publisher-safe primitives"
    - from: "bookkeeping_core/quotes.py"
      to: "bookkeeping_core/quote_publisher.py"
      via: "runtime quote capture stops mutating active facts directly and calls the publisher API"
    - from: "tests.test_postgres_backend"
      to: "tests.test_runtime"
      via: "publisher custody is proven both at persistence level and runtime entrypoint level"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py"
      provides: "Single guarded active-quote mutation entrypoint."
      contains: "class QuoteFactPublisher"
    - path: "wxbot/bookkeeping-platform/bookkeeping_core/database.py"
      provides: "Publisher-safe low-level mutation helpers without hidden commits."
      contains: "deactivate_old_quotes_for_group"
    - path: "wxbot/bookkeeping-platform/tests/test_postgres_backend.py"
      provides: "Publisher custody and transaction boundary regressions."
      contains: "publisher"
---

<objective>
Create the guarded publisher as the only core-owned fact-mutation API and make runtime quote capture call it instead of mutating active rows directly.

Output: a first-class `QuoteFactPublisher` plus publisher-safe DB primitives that no longer self-commit.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fact-protection-publisher/03-CONTEXT.md
@.planning/phases/03-fact-protection-publisher/03-RESEARCH.md
@.planning/phases/03-fact-protection-publisher/03-VALIDATION.md
@.planning/phases/06-constrained-auto-remediation-loop/06-VERIFICATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_core/quotes.py
@wxbot/bookkeeping-platform/bookkeeping_core/database.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce QuoteFactPublisher and central publish command contract</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py, wxbot/bookkeeping-platform/bookkeeping_core/quotes.py, wxbot/bookkeeping-platform/tests/test_runtime.py</files>
  <action>Create a dedicated publisher module in `bookkeeping_core/` with one obvious public API for active-quote mutation. The publisher should accept validator-owned publish inputs (`quote_document_id`, `publishable_rows`, owning group metadata, explicit publish mode) and return a structured result that can express applied/no-op/failure without mutating facts on its own by accident. Wire `QuoteCaptureService.capture_from_message()` to call this publisher instead of directly calling DB mutation helpers.</action>
  <acceptance_criteria>
Runtime no longer directly deactivates or upserts active quote rows.
There is one core-owned publish entrypoint that later web/routes can share.
Publisher input is validator/publishable-row oriented, not parser-hint oriented.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime -v</automated>
  </verify>
  <done>The repo now has one clear API that owns active-quote publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove hidden commits from quote mutation helpers so the publisher can own atomicity</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_core/database.py, wxbot/bookkeeping-platform/bookkeeping_core/quote_publisher.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <action>Refactor low-level quote mutation helpers used by the publisher so they no longer `commit()` independently. If helper splitting is needed, create publisher-private helper variants or explicit commit flags, but keep the public mutation custody readable. Add PostgreSQL-backed regressions proving these helpers can participate in one outer transaction owned by the publisher.</action>
  <acceptance_criteria>
No publish helper used by the publisher commits independently.
Publisher-level rollback can undo all quote mutations in one batch.
Regression tests fail if helper-level commits reappear and break atomicity.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime -v</automated>
  </verify>
  <done>Active-quote mutation helpers are now safe building blocks for an atomic guarded publisher.</done>
</task>

</tasks>

<threat_model>
- If runtime still calls DB mutation helpers directly, Phase 03 will only rename the bypass rather than removing it.
- If helper methods keep their own commits, later no-op/rollback guarantees will be fake.
- If publisher inputs trust parser-side publishability instead of validator-owned publishable rows, fact custody will remain ambiguous.
</threat_model>

<verification>
Wave 1 is complete only when runtime calls the guarded publisher and the publisher can own one atomic transaction boundary end-to-end.
</verification>

<success_criteria>
The codebase now has one guarded publisher API and no longer depends on hidden helper commits for active-quote mutation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fact-protection-publisher/03-01-SUMMARY.md`
</output>
