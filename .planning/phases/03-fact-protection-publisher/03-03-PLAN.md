---
phase: 03-fact-protection-publisher
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02-PLAN.md
files_modified:
  - wxbot/bookkeeping-platform/bookkeeping_web/app.py
  - wxbot/bookkeeping-platform/scripts/seed_quote_demo.py
  - wxbot/bookkeeping-platform/tests/test_postgres_backend.py
  - wxbot/bookkeeping-platform/tests/test_runtime.py
  - wxbot/bookkeeping-platform/tests/test_webapp.py
autonomous: true
requirements:
  - FACT-03
must_haves:
  truths:
    - "Routes, scripts, and agents do not have side doors around validator + publisher custody."
    - "Legacy replay/apply/delete surfaces either call the guarded publisher or are narrowed to fact-neutral proof flows."
    - "Architecture tests fail if forbidden low-level mutation helpers are called outside allowlisted files."
  key_links:
    - from: "bookkeeping_web/app.py"
      to: "bookkeeping_core/quote_publisher.py"
      via: "web replay/apply/delete actions now respect guarded publisher custody"
    - from: "scripts/seed_quote_demo.py"
      to: "tests.test_webapp"
      via: "demo/seed tooling no longer normalizes direct quote-row deletion as an accepted pattern"
    - from: "tests.test_runtime"
      to: "tests.test_webapp"
      via: "structural bypass prevention is enforced across runtime and UI surfaces"
  artifacts:
    - path: "wxbot/bookkeeping-platform/bookkeeping_web/app.py"
      provides: "Web actions narrowed to guarded publish or fact-neutral proof flows."
      contains: "/api/quotes/delete"
    - path: "wxbot/bookkeeping-platform/tests/test_webapp.py"
      provides: "Regression coverage for bypass route removal and proof-only wording."
      contains: "quotes/delete"
---

<objective>
Remove or block active-fact bypass paths outside the guarded publisher.

Output: web/routes/scripts/agents no longer have raw SQL or helper side doors into `quote_price_rows`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fact-protection-publisher/03-CONTEXT.md
@.planning/phases/03-fact-protection-publisher/03-RESEARCH.md
@.planning/phases/03-fact-protection-publisher/03-VALIDATION.md
@AGENTS.md
@wxbot/bookkeeping-platform/bookkeeping_web/app.py
@wxbot/bookkeeping-platform/scripts/seed_quote_demo.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route or disable web/script bypasses that currently touch quote_price_rows directly</name>
  <files>wxbot/bookkeeping-platform/bookkeeping_web/app.py, wxbot/bookkeeping-platform/scripts/seed_quote_demo.py, wxbot/bookkeeping-platform/tests/test_webapp.py, wxbot/bookkeeping-platform/tests/test_postgres_backend.py</files>
  <action>Replace raw delete/apply behavior in web routes and scripts with guarded-publisher calls where that behavior is still required, or disable the action if publisher semantics are not yet safe to express before Phase 04. Preserve proof-only replay wording where applicable. Any surviving mutation-capable route must be validator + publisher owned.</action>
  <acceptance_criteria>
`/api/quotes/delete` no longer deletes active rows directly.
Replay/result-save/delete flows either use the publisher or stay fact-neutral by design.
Seed/demo tooling no longer models direct `quote_price_rows` deletion as acceptable operational behavior.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime tests.test_webapp -v</automated>
  </verify>
  <done>Operators, scripts, and routes no longer have a raw side door into active quote facts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a structural bypass-guard test for forbidden low-level mutation callsites</name>
  <files>wxbot/bookkeeping-platform/tests/test_runtime.py, wxbot/bookkeeping-platform/tests/test_webapp.py</files>
  <action>Add an architecture-level regression that parses repo callsites and fails if forbidden low-level active-fact mutation helpers are invoked outside an explicit allowlist (for example the publisher module and tightly-scoped test setup). Cover runtime, web, and scripts. Keep the allowlist narrow and visible so future phases cannot quietly reintroduce direct fact mutation.</action>
  <acceptance_criteria>
The test suite fails if new direct calls to low-level quote-fact mutation helpers appear outside the allowlist.
Publisher custody is visible in code, not just in planning docs.
The bypass-guard test remains stable enough for future phases to extend rather than ignore.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/newlcc/SeeSee/repo/wxbot/bookkeeping-platform && BOOKKEEPING_TEST_DSN=postgresql://bookkeeping:password@127.0.0.1:5432/bookkeeping_test PYTHONPATH=. ./.venv/bin/python -m unittest tests.test_postgres_backend tests.test_runtime tests.test_webapp -v</automated>
  </verify>
  <done>Bypass prevention is now enforced structurally instead of depending on memory or review discipline.</done>
</task>

</tasks>

<threat_model>
- If web/routes/scripts keep raw delete paths, Phase 03 will only secure runtime while leaving operator shortcuts unsafe.
- If bypass prevention exists only as a comment, later phases can regress fact custody silently.
- If proof-only replay wording is not preserved, operators may confuse candidate proof flows with active publish flows.
</threat_model>

<verification>
Wave 3 is complete only when no active-fact bypass remains outside the publisher and structural tests catch forbidden mutation callsites.
</verification>

<success_criteria>
The codebase now enforces publisher custody across runtime, web, scripts, and future agent-facing surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fact-protection-publisher/03-03-SUMMARY.md`
</output>
